# Build Phase 7 deterministic benchmarks

## Objective

Replace author intent with executable proof. Build a Git-stored fixture corpus whose every case carries one controlled mutation, and executable oracles that prove each mutation had its expected effect before that case is allowed to count toward any metric. Only then can Phase 9 calibrate the classification policy's provisional thresholds against evidence instead of judgement.

## Problem

`CLASSIFICATION_POLICY_V2` (`src/domain/classification.ts:238`) ships `applicabilityMin: 0.5`, `sideMin: 0.65`, `criticalMin: 0.5`, and level cut points `[1, 2, 3]`. Those numbers have been provisional since Phase 4 and nothing has ever tested them against a known-correct answer. The only corpus that exists, `test/fixtures/discrimination/cart.test.ts`, labels its eight bad tests by author intent — a human wrote "this one is misleading" and nothing verified it. A threshold calibrated against intent is calibrated against the same judgement it is meant to replace.

## Why

Every quality claim this product makes rests on those thresholds. Until an operator and an oracle prove a case, a benchmark number is an opinion with a decimal point.

## Authorized scope

- Store the fixture corpus and its operator specifications in Git.
- Implement controlled mutation operators: removing an assertion, weakening an expectation, adding shared state, mocking owned logic, pinning an implementation detail, introducing uncontrolled time.
- Implement executable oracles that prove each operator's expected effect: production mutation, assertion mutation, semantics-preserving refactor, and repeated or randomized execution.
- Persist benchmark runs, oracle proofs, metrics, and comparisons; export JSONL behind an explicit flag.
- Report metrics per dimension.
- Do not implement the benchmark-review skill (Phase 8) or recalibrate any threshold (Phase 9).

## Scope and constraints

- **The never-execute guarantee is not weakened, it is bounded.** The audit pipeline still never executes audited source, package scripts, test runners, or configuration modules. Benchmark execution runs *our own* Git-stored fixtures, from a dedicated adapter, reachable only from an explicit benchmark command — never from `audit` under any flag. `test/evidence-resolution.test.ts:555` and `:571` (fixtures that throw if executed) and every `reportingOnly: true` assertion in `test/cli.test.ts` must keep passing untouched. A reader must be able to tell the two apart by which command they ran.
- Execution lives in an adapter. `src/domain` stays pure — no I/O, no timers, no process spawning — and `src/application` orchestrates through ports as it does everywhere else.
- **Benchmarks bypass the cache.** The cache key is pure content addressing (`src/adapters/cache-key.ts:78`), so a cached judgment for identical fixture bytes returns the first judgment ever recorded for them. Reusing it would measure the cache, not the model, and would make the PRD's required run-to-run stability metric impossible by construction. Benchmark sampling runs use `--fresh`.
- **Benchmark data does not live in the user's audit store.** Benchmark runs are keyed by Git fixture identity, not by an audited repository, and have a different lifecycle. Writing them into the store at the user's config home would mean a developer's benchmark work pollutes their own audit history. A separate database, under an explicit path, keeps the two apart.
- Recalibration is out of scope and stays out: per `docs/technical-design.md:156`, a new calibration is a new policy version such as `CLASSIFICATION_POLICY_V3`, never a silent edit of a shipped constant. Phase 7 produces the evidence; Phase 9 spends it.
- Only dimensions proven by an operator and an oracle count toward accuracy. An unproven observation stays `unverified` and must be visibly excluded from metrics, never quietly averaged in.
- Jev never labels its own benchmark.
- Artifacts use English. Preserve unrelated untracked `.atl/` files. Conventional Commits without AI attribution.

## Decisions

- A fixture case is a triple: a base test that genuinely passes against its production code, one operator applied to it, and the oracle that proves the operator did what it claims. Missing any leg, the case does not count.
- Oracle proof is recorded as data, not as a passing test run. A benchmark that re-derives its own ground truth on every invocation is a benchmark that can drift silently.
- The corpus lives in Git so a case's history is reviewable, and so a proof can be re-run against the exact bytes that produced it.

## Delivery

- Strategy: `auto-chain`.
- Forecast: approximately 4,500 authored changed lines, generated files excluded. Every prior phase overran its forecast; treat this as a floor.
- Chain strategy: cached `feature-branch-chain`.
- Tracker boundary: `feat/phase-7-benchmarks`, based on `main` at `dd0b454`.
- Planned local child slices: corpus and operators, executable oracles, benchmark persistence and comparison, per-dimension metrics and JSONL export.
- Remote tracker/child pull requests: not created; push and PR creation remain unauthorized remote operations.
- RDD: disabled/unmanaged.
- TDD: enabled by explicit user confirmation (carried forward); require observed RED, GREEN, REFACTOR, and critical mutation evidence.
- Test runner: Vitest.
- **API cost**: P7-1 and P7-2 make no provider call at all. Cost appears only from P7-3 onward, and the user decides the corpus size before the first sampling run.

## Primary references

- Implementation plan, work unit 7: `docs/implementation-plan.md:69`
- Technical design, "Deterministic evaluation": `docs/technical-design.md:332`
- PRD on benchmark reporting and blind review: `docs/PRD.md:148`
- The existing intent-labelled corpus this phase must supersede: `test/fixtures/discrimination/cart.test.ts`

## Acceptance criteria

**Verified demonstrably met, with evidence, as of P7-4 (the phase's last task) — not merely asserted:**

- Every corpus case carries a base test, exactly one operator, and a recorded oracle proof; a case missing any of the three is excluded from metrics and visibly reported as excluded. — **Met.** P7-1's `parseCorpusCaseManifest`/`buildCorpusCase` reject a manifest missing `testFile`, declaring two-or-zero operators, or missing `oracleKind` (`test/corpus.test.ts`, 26 cases, mutation-proven). P7-4's own `computeBenchmarkMetricsReport` excludes an unproven case from every dimension metric and reports it separately in `unprovenCases` with its reason (`test/benchmark-metrics.test.ts`, "only proven cases count"); a proven-but-unsampled case is likewise excluded and reported in `notSampledCases`.
- Each operator's expected effect is proven by execution, not asserted by a comment or a filename. — **Met.** P7-2's oracle runner spawns real, isolated vitest subprocesses against declarative mutations and records the observed pass/fail/timeout/error, never a static assertion (`test/oracle-corpus-integration.test.ts`, all 11 real cases proven exactly as declared, against the live corpus).
- An unproven case is reported `unverified` and never averaged into an accuracy figure. — **Met.** Structural since P7-1 (`proofStatus: 'unverified'` is the only literal the parser can assign, never read from a manifest); P7-3's `compareBenchmarkRuns` and P7-4's `computeBenchmarkMetricsReport` both filter on `proofStatus.kind === 'proven'` before any figure is computed, independently verified by dedicated mutation tests in both modules.
- Benchmark execution is unreachable from `audit` under every flag, and the existing never-execute tests pass untouched. — **Met.** `test/benchmark-cli-boundary.test.ts` now carries three positive/negative control pairs by static import-graph closure (oracle runner; audit store/cache key; P7-4's own `benchmark-metrics.ts`/`benchmark-jsonl-writer.ts`), all mutation-verified. `test/evidence-resolution.test.ts:555`/`:571` and every `reportingOnly: true` assertion in `test/cli.test.ts` re-run in isolation alongside the boundary suite: 165/165 passed, unchanged in substance across all four tasks.
- Benchmark data is written to its own database, never to the user's audit store, and no benchmark command writes anything without an explicit path. — **Met.** P7-3's `sqlite-benchmark-store.ts` uses its own meta-table name and refuses a real audit-store file outright (`BenchmarkStoreCorruptError`, proven by test and by a real file-mtime check). P7-4's `--jsonl <path>` writes nothing without that explicit flag (dedicated usage-error tests), mirroring `--html`'s own gating exactly, verified against both synthetic tests and a real 55-record export.
- Metrics are reported per dimension, and a dimension with no proven case reports that plainly rather than reporting zero. — **Met.** `computeBenchmarkMetricsReport` always returns all seven `RUBRIC_DIMENSION_IDS`; a dimension with `provenCaseCount: 0` (`behavioral-focus`, `diagnostic-quality` — structurally, since no corpus operator maps to either) reports `'not-computable'` with a named reason and `value: undefined` for every accuracy metric, never a numeric `0` — verified against both a deliberately non-vacuous synthetic fixture and the real corpus (this task's own real stability run).

## Tasks

- [x] **P7-1 — Store a fixture corpus with controlled operators**
  - Define the corpus format in Git: for each case, the base test, its production code, exactly one declared operator, and the operator's expected effect.
  - Implement the six operators as declarative specifications, not yet as executed mutations.
  - Supersede the intent-labelled discrimination fixture by expressing its cases in the new format, keeping the originals for comparison.
  - Verify: the corpus parses deterministically; a case declaring two operators or none is rejected; a declared expected effect is required; no provider call and no file write occur.
  - Delivered on `feat/phase-7-corpus` and committed as `908dd3e` (`feat: store a fixture corpus with declared operators`), then completed by `18ebabf` (`feat: state each corpus case prediction as data`) on `feat/corpus-expected-outcome`, which added the structured `expectedOutcome` and `operatorRole` fields. Route: direct-inline single-writer delegation.
  - Format decision, mid-task revision: the orchestrator asked, before any file existed, that production code be its own addressable per-case content (not inlined into the test, not a bare path) and that a case declare an expected effect on both the test side (what the operator changes about the test) and the production side (the falsifiable claim the oracle proves by acting on production), plus a required `oracleKind` naming which of the four `docs/technical-design.md:332` oracle kinds is meant to prove it. The shape below already stored production files as their own addressable entries; `testEffect`/`productionEffect`/`oracleKind` were added before any corpus file was written, so no format was retrofitted after the fact.
  - On-disk shape: one directory per case under `test/fixtures/corpus/<suite>/<case-id>/` — a `case.json` sidecar (`id`, `operators` (array, must resolve to exactly one), `oracleKind`, `testEffect`, `productionEffect`, `testFile`, `productionFiles`) plus the base test and every production file as real, byte-exact `.ts` files. Chosen over one manifest holding blob strings so a case's history is ordinary reviewable Git source (diffable, syntax-highlighted), and a later proof can be re-run against the exact bytes committed. Base-test files are named `test.ts`, never `*.test.ts`/`*.spec.ts`, so Vitest's default collection never executes them.
  - `src/domain/corpus.ts` (pure, zero non-relative imports, verified by `test/architecture-boundary.test.ts`): `parseCorpusCaseManifest` validates a manifest's raw JSON bytes (rejects invalid JSON, a non-object top level, any unknown field — including a hand-authored `"proofStatus"` — a missing/empty `id`, `operators` whose length is not exactly 1 or whose one entry is not a known operator id, a missing/unknown `oracleKind`, a missing/empty `testEffect`/`productionEffect`, and a `testFile`/`productionFiles` entry that escapes its case directory); `buildCorpusCase` combines a validated manifest with the adapter-read file bytes, cross-checks the paths match, and assigns `proofStatus: 'unverified'` itself (never read from disk, so a manifest cannot forge a proof).
  - `src/adapters/corpus-store.ts`: `loadCorpusCase` reads one case directory's bytes and calls the domain parser; `loadCorpusFromDirectory` reads every immediate subdirectory in sorted order (`readdir`'s own order is unspecified) and rejects a case whose manifest `id` does not match its own directory name (stricter than only rejecting duplicates, and it makes duplicates structurally impossible since directory names are already unique).
  - Unproven vs. proven is structural, not conventional: every `CorpusCase` P7-1 can produce carries the literal `proofStatus: 'unverified'`, and the type has no proof-shaped field at all yet — P7-2 adds the proven counterpart once an oracle can run. A case declaring an operator/oracleKind/effect pair with no proof reads as exactly "claim pending," never as a pass or a fail.
  - Ported the 11-case `test/fixtures/discrimination/cart.test.ts` (originals untouched) into `test/fixtures/corpus/discrimination/`, one directory per test, each with its own extracted single-test `test.ts` and a copy of the exact `cart.ts`/`audit-log.ts` bytes it needs. Operator assignment is this task's own analysis of what each test actually does (not a re-run of any oracle). `oracleKind` is declared independently per case from what its own `productionEffect` claim actually needs, not from a fixed operator→oracle table: the five cases whose only production-code hazard is an assertion too weak to reach it (`exposes-checkout-helper`, `works-boolean-check`, `computes-subtotal-truthy`, `discount-returns-number`, `checkout-tautology`) declare `production-mutation` (there is no meaningful assertion left to mutate — `expect(25).toBe(25)` has nothing an assertion-mutation oracle can act on); the two cases whose claim is specifically about the assertion's own strength on an otherwise-correct call (`subtotal-exact-value`, `discount-throws-range-error`) declare `assertion-mutation`; `mocks-discount-logic`/`checkout-applies-percent` declare `production-mutation`; `spies-on-math-round` declares `semantics-preserving-refactor`; `records-history-shared-state` declares `repeated-randomized-execution`, with its `productionEffect` rewritten to a claim that oracle can actually check (two runs sharing one loaded module, not a mid-test state reset the four named oracle kinds have no mechanism for). An earlier draft assigned `oracleKind` from operator identity alone and left 5 of 11 cases contradicting their own `productionEffect`; caught and corrected before reporting, not left in the corpus. **Gap, returned rather than resolved:** the ported set never uses `introduce-uncontrolled-time` (the original fixture's nondeterministic case uses unseeded randomness and shared module state, not real wall-clock time) — orchestrator to decide whether to author a new case for it or accept the gap. **`operator` means something different depending on which of the two groups a case is in — a P7-2 design input to return, not smooth over.** For the 3 good-control cases, `operator` is *prescriptive*: P7-2 is expected to actually apply it (apply the named operator to the base test) to produce a variant, then compare the base test and that variant against one production mutation — `productionEffect` is a claim about that comparison. **Correction:** only 2 of these 3 (`subtotal-exact-value`, `discount-throws-range-error`) are `assertion-mutation`; the third, `checkout-applies-percent`, is `production-mutation` — the same oracle kind as several *descriptive* cases below — and is prescriptive anyway. That is exactly why `oracleKind` cannot carry the prescriptive/descriptive distinction on its own, contrary to what the original bullet here implied. For the other 8 cases, `operator` is *descriptive*: the flaw it names is already present in the base test as written, P7-2 runs the named oracle against the base test unmodified, and never applies the operator at all — `productionEffect` is a claim about the base test alone. `oracleKind` was assumed at the time of this bullet's original writing to be what tells P7-2 which of the two a given case is; the Correction above shows that assumption doesn't hold for `checkout-applies-percent`. Nothing in the manifest stated the distinction more directly than that now-superseded inference, which is exactly the ambiguity a future P7-1 revision (or P7-2 itself) may want to make an explicit field rather than an inference from `oracleKind` — resolved by the bullet directly below.
  - **Resolved (follow-up change, branch `feat/corpus-expected-outcome`):** every case now also declares `operatorRole` (`'prescriptive'` | `'descriptive'`, explicit, required, structural — no longer inferred from `oracleKind`) and `expectedOutcome` (`'expected-to-fail'` | `'expected-to-keep-passing'`, a raw, oracle-kind-agnostic prediction of the base test's own pass/fail outcome under its declared oracle, quoting the tail of each case's own `productionEffect` claim). Both are required and validated exactly like `oracleKind`, closing this ambiguity without relying on `oracleKind` as a proxy. `expectedOutcome` is deliberately not a goodness signal: `spies-on-math-round` (`'descriptive'`, deliberately bad) predicts `'expected-to-fail'`, the same value the 3 `'prescriptive'` good controls predict, because a test that pins an implementation detail breaks on a behavior-preserving refactor for the wrong reason.
  - TDD: strict RED-implement-GREEN per module, observed directly (see this report's mutation-evidence log for exact commands/output). Adapter (`src/adapters/corpus-store.ts`) was genuinely test-first: `test/corpus-store.test.ts` (7 cases) written and run RED (`Cannot find module '../src/adapters/corpus-store.js'`) before the adapter existed; implemented once, GREEN on the first run. Domain module (`src/domain/corpus.ts`) was drafted first by mistake, caught before any test ran against it, moved out of `src/domain` so `test/corpus.test.ts` (26 cases) could be written and run genuinely RED against a missing module (`Cannot find module '../src/domain/corpus.js'`), then restored unmodified; GREEN on the first run after restoring (26/26) — disclosed here rather than described as clean test-first authorship it was not.
  - Mutation evidence (each: break, observed RED, restore, observed GREEN, confirmed byte-identical restore via `diff`). First attempt, disclosed rather than discarded: swapping the `operator` value directly between two of `test/corpus.test.ts`'s own `DISTINCT_FIXTURES` entries **stayed GREEN** — that fixture array is read for both the parser's input AND the test's expected value, so moving the two together proves nothing about the parser. Corrected to: (1) fed a hardcoded `operators: ['remove-assertion']` into every synthetic fixture's input JSON while leaving assertions pointed at each fixture's real operator (decoupling input from expectation) — 5/6 per-fixture assertions went RED, proving the parser threads the declared operator through rather than returning a constant; (2) removed the `operators.length !== 1` check — RED on both "rejects two operators" and "rejects no operators"; (3) removed the non-empty check shared by `testEffect`/`productionEffect` — RED on both; (4) removed the unknown-top-level-key rejection — RED on the `"proofStatus"` smuggling test; (5) removed the `..`/absolute-path rejection in `isSafeRelativePath` — RED on both the `testFile`- and `productionFiles`-escape tests; (6) removed `buildCorpusCase`'s declared-vs-received path cross-check — RED on all three of its mismatch tests; (7) in the adapter, removed the sorted-order sort before parsing — **stayed GREEN on this filesystem** (`readdir` already returns alphabetical order on this APFS volume in this test), so that specific test does not by itself pin ordering on every OS — the sort is still correct defense-in-depth (Node's `readdir` order is unspecified), but this is disclosed rather than claimed as proven-by-mutation; (8) removed the adapter's manifest-id-vs-directory-name check — RED; (9) swapped `operators` between two *real, Git-stored* `case.json` files (`exposes-checkout-helper` and `spies-on-math-round`), added first a dedicated assertion pinning every real case's operator and oracleKind (`test/corpus-store.test.ts`, "parses each real discrimination case with its own declared operator and oracleKind") — RED, then restored both files byte-identical (verified with `diff`) — GREEN.
  - Verification: `npx vitest run` 1020 passed across 38 files (987 baseline + 33 new: 26 + 7); `npm run typecheck` clean; `npm run lint` clean; `npm run build` clean; `git diff --check` clean. `test/evidence-resolution.test.ts` and `test/cli.test.ts` re-run in isolation, unchanged, 155/155 passed.
  - Files: `src/domain/corpus.ts` (+299), `src/adapters/corpus-store.ts` (+78), `src/index.ts` (+14, new exports only), `test/corpus.test.ts` (+246), `test/corpus-store.test.ts` (+158), `test/fixtures/corpus/discrimination/**` (33 files, +343 lines: 11×`case.json` + 11×`test.ts` + 10×`cart.ts` + 1×`audit-log.ts`), `docs/technical-design.md` (+8/-0), this file (+21/-5, net edits to the P7-1 task entry, Progress, and Slice ledger). Authored total ≈1,162 lines (`git diff --stat` on tracked files + `wc -l` on new files).
- [x] **P7-2 — Prove each operator by execution**
  - Implement the oracle runner in a dedicated adapter that spawns a test runner against corpus fixtures only, reachable from an explicit benchmark command and never from `audit`.
  - Record each proof as data: which operator, which oracle, what was observed, and whether the expected effect held.
  - Verify: a case whose mutation does not produce its expected effect is reported unproven rather than silently counted; the audit path cannot reach the runner; the never-execute tests pass untouched; execution is bounded and cannot hang a run.
  - Delivered on branch `feat/phase-7-oracles`, created from `feat/corpus-expected-outcome`@`18ebabf` — this task's own actual starting point, where P7-1's corpus format, parser, and fixture corpus are already committed (see this document's Progress section for the discrepancy this revealed against the plan this document previously recorded, and the branch reconciliation left open for the orchestrator). Committed nowhere — branch created and left with working-tree changes, per this task's own explicit instruction not to commit, push, or switch branches once created. Route: direct-inline single-writer delegation (one bounded task, not SDD).
  - **Architecture** (`docs/technical-design.md`'s "Deterministic evaluation" section has the full delivery note): `src/domain/oracle.ts` (pure — `buildOraclePlan` turns a `CorpusCase` into an `OraclePlan`; `decideProof` judges recorded observations; declarative `TextTransform` mutations, anchor-checked exactly-once) / `src/adapters/oracle-runner.ts` (the one process-spawning adapter: scratch dir under `os.tmpdir()`, symlinked `node_modules`, isolated vitest subprocess, explicit allow-listed env, timeout + process-group `SIGKILL`, vitest's own JSON reporter read to tell a genuine failure from a load error) / `src/application/benchmark.ts` (`proveCase`/`proveCorpus`, orchestrating through one injected `OracleObservationPort`, exactly like `runAudit`/`AuditPorts`) / `src/cli/benchmark.ts` (its own CLI entry, never imported by `src/cli/index.ts`).
  - **Execution boundary, proven not asserted**: `test/benchmark-cli-boundary.test.ts` walks the static TypeScript import graph (the same technique `test/architecture-boundary.test.ts` already uses) from `src/cli/index.ts` and asserts `src/adapters/oracle-runner.ts`/`src/cli/benchmark.ts` are absent from that closure, *and* — a positive control, so the negative assertion cannot pass vacuously — that the closure from `src/cli/benchmark.ts` DOES contain the runner. Verified non-vacuous directly: a temporary bad import was added to `src/cli/index.ts`, the boundary test observed RED, the import was reverted, `git diff --stat src/cli/index.ts` confirmed byte-identical restoration, and the test was re-run GREEN.
  - **Oracle-kind mechanics, derived from the 11 real cases' own `productionEffect` prose, not assumed from the label**: every `'descriptive'` case (`production-mutation` or `semantics-preserving-refactor`) is a `baseline` + one mutated run of the unmodified base test. Every `'prescriptive'` case (`production-mutation` or `assertion-mutation` — `checkout-applies-percent` is `production-mutation` yet prescriptive, exactly the case P7-1's own report flagged as proof `oracleKind` cannot carry the role distinction) runs the SAME two-run comparison regardless of its `oracleKind` label: base test vs. one fixed production mutation (expected to flip to failing), then the operator-derived variant vs. the SAME mutation (expected to keep passing, showing the operator hides the defect). `'repeated-randomized-execution'` (`records-history-shared-state`, the only real case) is the one genuinely different shape: its `describe(...)` block is duplicated so two `it()`s share one module load, against a production mutation (`record-only-first-call`) that only breaks `record` from the second call onward — proving the first call's leftover module state masks the second call's broken one.
  - **`decideProof`'s reasons** (never a bare boolean): `baseline-failed`, `incoherent-declaration` (a `'prescriptive'` case declaring `'expected-to-keep-passing'` — decided before even looking at the mutation/variant observations, since no execution result could make that declaration coherent), `<run> timed-out` / `<run> runner-error: ...`, `prediction-not-held`, `operator-did-not-hide-defect` (the variant also failed — the operator did not, in fact, hide the defect), or `{ kind: 'proven' }`.
  - **Never mutates the corpus — structural, not disciplined**: every mutation is a pure string transform in `src/domain/oracle.ts`, which cannot do I/O at all (enforced by the same `test/architecture-boundary.test.ts` non-relative-import ban every domain module is held to); only `src/adapters/oracle-runner.ts` touches disk, and it only ever writes into one `fs.mkdtemp` scratch directory under `os.tmpdir()`, never `test/fixtures/corpus/`. Verified twice: (1) `fs.rmSync(scratchDir, { recursive: true, force: true })` was checked directly, before any adapter code was written, against a scratch directory containing a symlinked `node_modules`, confirming the symlink is unlinked and its real target survives untouched; (2) `test/oracle-corpus-integration.test.ts` sha256-hashes every file under `test/fixtures/corpus/discrimination/` before and after a full 25-subprocess proof run (including a case constructed to be unrealizable) and asserts byte-identical, plus asserts zero leftover scratch directories under this port instance's own randomized prefix.
  - **Bounded execution**: a synthetic `while (true) {}` fixture (a real busy loop — an unresolved Promise would be caught by vitest's OWN `testTimeout` instead, never exercising this adapter's own kill path) is killed by `process.kill(-child.pid, 'SIGKILL')` against a `detached: true` process group, so any worker processes vitest itself might spawn die too; the observed result is `'timed-out'`, never `'failed'`. Measured wall-clock elapsed time on the kill test is asserted well under vitest's own 5s default per-test timeout, proving THIS adapter's timeout fired, not vitest's.
  - **An explicit, minimal child environment** (`buildChildEnv`): allow-lists `PATH`/`HOME`/`TMPDIR`/`TEMP`/`TMP`/`SystemRoot`/`USERPROFILE` plus a fixed `NO_COLOR=1`, never a spread of `process.env` — the outer suite's own `vitest` process sets `VITEST`/`VITEST_WORKER_ID`/`NODE_ENV`, which a spread would leak into the spawned child (exactly the "child that inherits the parent's config... can pass while proving nothing" risk this task's own instructions named). Verified directly: a mutation that spread `process.env` instead was confirmed to leak `VITEST`/`NODE_ENV`/`CI` through, RED, then reverted, GREEN.
  - **Module resolution without a copied `node_modules`**: the scratch directory gets one symlink, `node_modules -> <this package's real node_modules>`, resolved via `import.meta.resolve('vitest/package.json')` (never a hardcoded `node_modules/vitest/vitest.mjs` path). `vitest`'s own JSON reporter shape (`assertionResults`, `status`, `failureMessages`, per-file `message` for a load error) was determined empirically with a throwaway spike (real spawn, real pass/fail/syntax-error/timeout/relative-import cases) before any adapter code was written, rather than assumed from documentation.
  - **`vitest` stays a devDependency, not added to `dependencies`** (open decision, not silently changed): the benchmark command is a maintainer/development surface run against a full `npm ci` checkout, not something an installed end-user build of `jev-test-auditor` needs.
  - **No `package.json` `bin` entry for `benchmark`** (open decision, not made here): runs today via `node dist/cli/benchmark.js` after `npm run build`.
  - **Case id -> mutation recipe lives in a domain-side registry in `src/domain/oracle.ts`**, not a new `case.json` field (open decision, not made here): retrofitting P7-1's manifest shape and parser was out of this task's authorized scope.
  - **Three findings from an independent design review (advisor), fixed before reporting done**: (1) the CLI entry guard originally compared `import.meta.url` to a raw, non-percent-encoded `process.argv[1]` — silently never matching on a path containing a space; fixed to mirror `src/cli/index.ts`'s own `isInvokedAsPackageEntry` (`realpathSync`/`fileURLToPath` comparison) exactly. (2) the spawned child's `stdout` was piped but never read; a fixture that prints heavily (a realistic mutation outcome — a logging loop) could fill the OS pipe buffer and block the child, which this adapter would misreport as `'timed-out'` rather than the pipe-backpressure bug it actually was. Fixed to `stdio: ['ignore', 'ignore', 'pipe']` (stdout ignored outright — the adapter only ever reads the outcome from vitest's own `--outputFile` JSON report, never stdout). Disclosed rather than overclaimed: a live mutation test for this specific scenario (~100KB of `console.log` output before a pass) did **not** reproduce a hang even with the old piped setting on this machine — Vitest appears to capture per-test `console.*` output internally rather than streaming it live through the child's real stdout file descriptor, so the fix is defensive/structural rather than proven-necessary-by-reproduction here; kept because it costs nothing (stdout content is never needed) and removes the risk class entirely regardless of Vitest's exact internal buffering behavior, current or future. (3) the timeout path's `process.kill(-child.pid, 'SIGKILL')` had no fallback if that call threw for a reason *other than* "already exited" (e.g. negative pids are unsupported on Windows) — such a throw would have left the child running forever, `observe()` hanging, the exact "cannot hang a run" violation the acceptance criterion names; added a `child.kill('SIGKILL')` fallback in the catch.
  - **Windows disclosed, untested** (same posture `src/adapters/html-report-opener.ts` already takes for its own untested win32 path): this task's process-group kill (`process.kill(-pid, ...)`, negative pid) and the scratch directory's `node_modules` symlink (`fs.symlink(..., 'dir')`, which needs elevated privileges or Developer Mode on Windows) are both exercised only on this machine's macOS/POSIX path. The fallback in finding (3) above softens, but does not remove, the Windows kill risk.
  - **Verified as a real process, not only through the test harness**: after `npm run build`, `node dist/cli/benchmark.js` (no arguments, default corpus path) was run directly — `11/11 case(s) proven.`, one `PROVEN` line per real case naming its operator/oracleKind, exit code `0`, ~6s wall clock. `node dist/cli/benchmark.js --bogus` and `node dist/cli/benchmark.js --corpus` (missing its value) were also run directly, each printing its usage error and exiting `1`. `git status` confirmed `test/fixtures/corpus/` unchanged after the real run, independent of the automated corpus-hash test.
  - TDD: strict RED-implement-GREEN, observed directly. `test/oracle.test.ts` (19 cases) and `test/oracle-runner.test.ts` (9 cases, including two real subprocess spawns per test) were both written against not-yet-existing modules and run RED (`Cannot find module '../src/domain/oracle.js'` / `'.../oracle-runner.js'`) before either module existed; each was then implemented once and passed GREEN on the first run against its own test file. `test/oracle-corpus-integration.test.ts` and `test/benchmark-cli-boundary.test.ts`/`test/benchmark-cli.test.ts` were written and passed once the modules they exercise already existed (an integration/static-check layer over already-TDD'd units, not itself given a separate RED phase) — disclosed rather than described as TDD it was not; the boundary test's own true/false discrimination is instead proven by the live mutation described above.
  - Mutation evidence (each: break, observed RED, restore, observed GREEN — `git diff --stat`/re-run confirmed byte-identical restoration throughout): `applyTextTransform`'s ambiguous-anchor check (`!== 1` → `< 1`) — RED on "throws when the anchor occurs more than once" only; `decideProof`'s prescriptive-incoherence short-circuit disabled — RED on the incoherent-declaration case, correctly falling through to a DIFFERENT (also-wrong) reason rather than silently proving; `duplicateDescribeBlock`'s `.repeat(times)` → no-op — RED on both tests that depend on it; the adapter's `context.timedOut` classification disabled — RED on the real timeout test, which fell through safely to `runner-error` rather than hanging (proving the underlying kill still fired); the adapter's `expectedTestCount` mismatch check disabled — RED on both the syntax-error and too-few-results tests; `buildChildEnv`'s allow-list replaced with a `process.env` spread — RED, leaking `VITEST`; the CLI boundary's negative assertion — RED when `src/cli/index.ts` was given a real (temporary) import of the runner.
  - Verification: `npx vitest run` 1062 passed across 43 files (1027 baseline + 35 new: 19 + 10 + 1 + 2 + 3 — one extra adapter test, the stdout back-pressure regression from finding (2) above); `npm run typecheck` clean; `npm run lint` clean; `npm run build` clean (confirmed `dist/cli/benchmark.js` was actually emitted); `git diff --cached --check` clean (new files staged only for this check, then unstaged — nothing committed). `test/evidence-resolution.test.ts`, `test/cli.test.ts`, and `test/architecture-boundary.test.ts` re-run together in isolation: 159/159 passed, unchanged. `git status` after both the full suite run and the real `node dist/cli/benchmark.js` process run shows `test/fixtures/corpus/` with no changes; `.gitignore` carries only its pre-existing modification from before this task started, untouched by this task per its own instruction.
  - Files: `src/domain/oracle.ts` (+508), `src/adapters/oracle-runner.ts` (+315), `src/application/benchmark.ts` (+109), `src/cli/benchmark.ts` (+110), `test/oracle.test.ts` (+215), `test/oracle-runner.test.ts` (+160), `test/oracle-corpus-integration.test.ts` (+92), `test/benchmark-cli-boundary.test.ts` (+92), `test/benchmark-cli.test.ts` (+63), `README.md` (+2/-2), `docs/technical-design.md` (+12/-0), this file (net edits to the P7-2 task entry, Progress, and Slice ledger). Authored total ≈1,678 lines (`wc -l` on the 9 new source/test files + `git diff --stat` on the 2 modified tracked docs).
  - **Gap, returned rather than resolved**: P7-1's own open gap (`introduce-uncontrolled-time` unexercised by the ported corpus) is still open — P7-2 proves the 11 cases as declared, and adds no new case.
- [x] **P7-3 — Persist benchmark runs and comparisons**
  - Add a separate benchmark database with its own migrations, storing runs, operator identities, oracle proofs, and per-case outcomes.
  - Compare two benchmark runs by fixture identity, reporting agreements, disagreements, and regressions.
  - Verify: a fresh database migrates from empty; the user's audit store is never touched; a comparison across differing rubric or policy versions is refused or clearly labelled rather than silently compared.
  - Delivered on branch `feat/phase-7-benchmark-store`, created from `feat/phase-7-oracles` (which was already fully committed as `66a3e9d` — the task's own note that P7-2 was "committed nowhere" was stale by the time this task started; verified directly with `git log`/`git show --stat` before doing anything else). Committed nowhere — per this task's explicit instruction not to commit, push, or switch branches once created, every change below is uncommitted working-tree state. Route: direct-inline single-writer delegation (one bounded task).
  - **A benchmark run samples Jev's verdict for each case AND records P7-2's own oracle proof beside it — never one derived from the other** (Decisions: "Jev never labels its own benchmark"). `src/application/benchmark-run.ts`'s `runBenchmarkPass` calls `proveCase` (P7-2, reused unchanged) and a new `BenchmarkSamplePort.sample` independently for every case, then — only when a store is supplied — persists the combined `BenchmarkCaseRecordInput` per case, exactly mirroring `runAudit`'s own opt-in `beginRun`/`recordWorkItem`/`finishRun` bracketing (`src/application/audit.ts`).
  - **Sampling reuses the real `audit --evaluate` pipeline, never a second hand-rolled one.** `src/adapters/benchmark-sample-port.ts`'s `createBenchmarkSamplePort(corpusDir, gateway)` calls `runAudit` (`src/application/audit.ts`) pointed at one corpus case's own on-disk directory (`join(corpusDir, corpusCase.id)`, safe because P7-1's loader already guarantees `id === directory name`), with `resolveConfiguration({ rootDir: caseDir, include: [corpusCase.baseTest.path], concurrency: 1 })` — the explicit `include` override needed because discovery's own default glob is `*.test.ts`/`*.spec.ts`, which a bare `test.ts` does not match. Verified empirically, not assumed (an advisor-recommended spike, done before writing the adapter): this repository's own `TEST_NAME_PATTERN` (`src/adapters/repository-discovery.ts`) — distinct from Vitest's OWN default collection glob, which is what P7-1's doc was actually about — DOES match a bare `test.ts` (`node -e` regex check, then confirmed for real against `checkout-applies-percent` with a stub gateway, `test/benchmark-sample-port.test.ts`).
  - **Sampling bypasses the cache structurally, not merely via `--fresh`.** The `AuditPorts` `createBenchmarkSamplePort` builds carry no `store` and no `cacheKey` at all — `runAudit`'s cache lookup only ever runs when both are present, so there is no code path here that could consult a cache even in error. `--fresh: true` is passed anyway as documentation and defense-in-depth. `jestFrameworkHint` is also omitted (every corpus fixture already imports `vitest` explicitly, so import-based attribution always succeeds; reading a `package.json` outside the case directory would be unneeded I/O). Proven by test: two samples of the identical case, against a gateway returning a genuinely distinguishable answer each call (different score AND different probability mass — an earlier draft of this test used an all-zero probability distribution that made both calls come back `needs-review` regardless of dispatch count, which would have passed the "gateway called twice" assertion while proving nothing about non-caching; caught and corrected before this task was reported done, not left in the suite), both reach the gateway and return different classifications (`misleading` then `healthy`).
  - **Never touches the user's audit store — proven three independent ways, not just asserted.** (1) Structurally: nothing in `benchmark-sample-port.ts` or `sqlite-benchmark-store.ts` imports `sqlite-audit-store.ts` at all — extended `test/benchmark-cli-boundary.test.ts` with a positive/negative closure pair symmetrical to its existing oracle-runner one: `src/cli/index.ts`'s closure DOES reach `sqlite-audit-store.ts`/`cache-key.ts` (positive control), `src/cli/benchmark.ts`'s closure never does — verified by mutation (a temporary bogus `import type { AuditStorePort } from '../adapters/sqlite-audit-store.js'` added to `benchmark.ts` was observed to turn the negative assertion red, then reverted, byte-identical, confirmed green). (2) By unit test: a real audit store, created via `createSqliteAuditStore` under a temp `XDG_CONFIG_HOME`, hashed, then `createBenchmarkSamplePort(...).sample(...)` called — the file is byte-identical after (`test/benchmark-sample-port.test.ts`, and again end-to-end through the CLI in `test/benchmark-cli-store.test.ts`). (3) By the real end-to-end run (see below): the real, pre-existing `~/.config/jev-test-auditor/audit-store.sqlite3`'s own filesystem modification time was checked before and after both real benchmark CLI invocations and is unchanged, matching what `git status` (scoped to the repository) independently confirms for the tracked audit store code path.
  - **A distinct meta table name is the concrete mechanism, not a convention.** `sqlite-store-common.ts`'s `SqliteStoreSchema.metaTableName` is per-store identity: the benchmark store uses `benchmark_schema_meta`, never the audit store's `schema_meta`. `test/sqlite-benchmark-store.test.ts`'s own dedicated test creates a REAL audit store via `createSqliteAuditStore`, opens it through `createSqliteBenchmarkStore`, and asserts it is refused with a named `BenchmarkStoreCorruptError` (a real "foreign database — has user tables, no `benchmark_schema_meta` table" detection, the audit store's own existing P5-1 mechanism, reused unmodified) and left byte-identical. Verified by mutation: temporarily renaming the benchmark schema's meta table to `schema_meta` (the audit store's own name) turned that test red (the wrong error TYPE was thrown — `BenchmarkStoreSchemaVersionError`, since the shared version-3 audit schema would then be misread as a too-new BENCHMARK schema — proving the distinct name is what keeps the error legible, not merely present), then reverted.
  - **Reused from the audit store, not rewritten**: the versioned-migration engine (schema_meta bookkeeping, transactional migration application, idempotent re-open, foreign-database detection, native-error wrapping) and the `node:sqlite` `ExperimentalWarning` suppression (module-level `suppressionDepth` counter) were extracted verbatim from `src/adapters/sqlite-audit-store.ts` into a new `src/adapters/sqlite-store-common.ts`, parameterized by `{ metaTableName, schemaVersion, migrations }` and a store's own error-factory pair — never copy-pasted a second time. The warning suppression specifically *had* to move, not merely be shareable: its overlap-safe design is correct only with exactly one `suppressionDepth` counter in the whole process; two independent copies (one per store module) would each patch/restore `process.emitWarning` unaware of the other's active window, reintroducing the exact cross-call race the design exists to prevent — now across stores instead of across overlapping calls to one store. The audit store now delegates to the shared engine and re-exports `isSqliteExperimentalWarning`/`withSqliteExperimentalWarningSuppressed` unchanged, so every existing import keeps working. Proof this refactor changed no behavior: the audit store's own full, pre-existing 1579-line/67-test suite (`test/sqlite-audit-store.test.ts`) was run before touching a line, then run again after the extraction with zero changes needed — 67/67 passed both times, byte-for-byte the same test file.
  - **Rewritten, genuinely new to this task**: `src/domain/benchmark-store.ts` (types, port, error classes — mirrors `AuditStorePort`'s own shape in `src/domain/audit.ts`), `src/domain/benchmark-comparison.ts` (the comparator), `src/application/benchmark-run.ts` (orchestration), `src/adapters/sqlite-benchmark-store.ts` (its own five tables, its own schema, its own fixture-hash computation), `src/adapters/benchmark-sample-port.ts` (the sampling pipeline wiring), `src/cli/api-key.ts` (see below), and the CLI extension in `src/cli/benchmark.ts`.
  - **`BenchmarkCaseRecordInput` carries raw `fixtureFiles`, never a precomputed `fixtureHash`, on the write side** (`src/domain/benchmark-store.ts`): `src/application` is held to the identical non-relative-import ban `test/architecture-boundary.test.ts` enforces on `src/domain` (confirmed by reading that test's own `hasForbiddenDependency` — it rejects ANY non-relative specifier for `application`, not only the cross-layer segments), so `runBenchmarkPass` cannot call `node:crypto` itself. The SQLite adapter computes the sha256 fixture hash (sorted by path, `path:<path>\0<contents>\0` per file — the same anchor-style serialization `oracle-runner.ts`'s own `hashRun` uses, computed independently rather than reusing that function, since a corpus case's own identity and one particular oracle run's mutated file set are deliberately different things that happen to share a hashing technique, not the same hash space).
  - **"Fixture identity" is id *and* bytes *and* declaration, not id alone** (`src/domain/benchmark-comparison.ts`'s `sameFixture`): a matched case whose `fixtureHash` differs, OR whose `operator`/`operatorRole`/`oracleKind`/`expectedOutcome` declaration differs (even with byte-identical test/production files — a `case.json` edit with no file change is still a changed fixture), is `'fixture-changed'`, excluded from every bucket. Proven by two separate tests, both required (a mutation disabling only the byte check, or only the declaration check, would each leave the other passing).
  - **Only proven cases count, computed exactly where `decideProof` already establishes the guarantee** — not merely filtered at the edge. `isCaseCorrect(operatorRole, status)` (a coarse, binary healthy/not-healthy signal — `'prescriptive'` correct iff `healthy`, `'descriptive'` correct iff NOT `healthy`) is applied only to cases `CaseProofStatus` `'proven'` in BOTH runs, and is trustworthy exactly because `decideProof` already confirmed the underlying claim by execution (for a prescriptive case: its base test genuinely catches the fixed mutation its own operator-derived variant hides; for a descriptive case: its declared flaw's `expectedOutcome` actually held). This mapping reads directly off the corpus's own already-documented design (`src/domain/corpus.ts`'s module doc: "3 good controls and 8 deliberately bad, one per rubric criterion") rather than inventing new quality policy for this task — flagged here explicitly as a decision, not silently assumed, per this task's own instruction to return rather than invent product choices; P7-4's own future per-dimension precision/recall is a separate, finer-grained question this comparator does not answer.
  - **Regression, agreement, and disagreement, disambiguated** (`compareBenchmarkRuns`): identical `status` between the two runs is an agreement; a differing `status` is ordinarily a disagreement UNLESS it is specifically a flip from correct to incorrect (`baselineCorrect && !candidateCorrect`), which is a regression — so a descriptive case moving `weak` -> `misleading` (still correctly non-healthy both times) is a disagreement, never miscounted as a regression, while a prescriptive control moving `healthy` -> `weak` (correct -> incorrect) is. Verified by one test asserting all three buckets individually from one hand-built pair of runs with an agreement, a non-regressing disagreement, and a regression each carrying distinct caseIds and statuses (never a symmetric fixture that could pass by construction) — plus a dedicated mutation (disabling the regression branch entirely) confirmed red on exactly that test, nothing else.
  - **Refuse, not label, on a rubric- or policy-version mismatch — the decision this task's own instructions asked to be made and justified.** `compareBenchmarkRuns` derives each run's own `(policyVersion, rubricVersion)` from its samples (never a separately-tracked field that could drift from what was actually sampled — refusing outright, with a named reason, if a run has zero successful samples or more than one distinct pair among them), and refuses the WHOLE comparison — zero agreements, zero disagreements, zero regressions, nothing a caller could mistake for a real number — the moment the two runs' versions differ on either axis. Chosen over labelling because this module has no vocabulary yet for representing "this delta is confounded by a version change" in a way a caller could safely still treat the accompanying regression count as real; a caller skimming past a label next to a still-populated regression list is exactly the "false result with a decimal point" the hard constraint warns about. Refusing is the harder guarantee: there is exactly one code path, and a mismatched pair cannot produce a regression list under any circumstance, accidentally or otherwise. A **model** identity mismatch is deliberately NOT part of this refusal — the PRD explicitly wants "regressions between rubric or model versions" as a future capability — so it is labelled (`modelMismatch`, `baselineModel`, `candidateModel`) and the comparison still proceeds; this is the one place refuse-vs-label genuinely splits by design rather than defaulting to the same answer everywhere.
  - **The CLI** (`src/cli/benchmark.ts`): bare invocation is unchanged from P7-2 byte-for-byte — the store-less code path was left completely untouched, not merely behaviorally equivalent, specifically so this task could not have silently drifted it (verified: `node dist/cli/benchmark.js` with no arguments still prints `11/11 case(s) proven.` and exits 0). `--store <path>` is the sole gate for both sampling and persistence, mirroring how `--html <path>` gates writing a report file: an API key is resolved strictly before anything is sampled or any database file is opened, so a missing key fails with nothing written (verified by test, including that the store file genuinely does not exist afterward). `--baseline <runId> --candidate <runId>` (also requiring `--store`, and rejecting `--corpus`/`--timeout-ms` in combination) is a second, read-only mode that never proves or samples anything new.
  - **`resolveEvaluationApiKey`/`NO_KEY_USAGE_MESSAGE` moved to a new `src/cli/api-key.ts`**, imported by both `cli/index.ts` and `cli/benchmark.ts`, rather than benchmark importing them from `cli/index.ts` directly — the concrete reason, not merely tidiness: importing from `cli/index.ts` would pull its ENTIRE import graph (the audit store, the cache-key port, the whole `audit` command surface) into `benchmark.ts`'s own closure, silently breaking the "benchmark never reaches the audit store" proof this same task adds. `cli/index.ts`'s own behavior is unchanged — verified by re-running its full 104-test suite (`test/cli.test.ts`) after the extraction with zero test changes needed.
  - TDD: strict RED-implement-GREEN throughout, every new test file observed genuinely failing (`Cannot find module ...`) before its module existed, then implemented once and run GREEN — `test/benchmark-comparison.test.ts` (14 cases, one iteration needed to fix a self-inconsistent test fixture, not the implementation — see mutation-evidence note above), `test/benchmark-run.test.ts` (4), `test/sqlite-benchmark-store.test.ts` (10, one iteration needed to fix a test fixture whose "identical bytes" premise was accidentally false because it templated file contents from the caseId under test), `test/benchmark-sample-port.test.ts` (4, one iteration needed to fix the cache-bypass test's own stub gateway — see above), `test/benchmark-cli-store.test.ts` (11). `test/sqlite-audit-store.test.ts` (the extraction's regression net) was run before AND after the refactor with zero source changes needed to keep it green.
  - Mutation evidence (each: break, observed RED, restore, observed byte-identical GREEN): `compareBenchmarkRuns`'s regression branch disabled — RED on the regression-bucketing test only; `sameFixture`'s byte-hash check disabled — RED on both fixture-changed tests; `compareBenchmarkRuns`'s policy-version-mismatch check disabled — RED on that refusal test only; `runBenchmarkPass`'s sample/sampleFailure branch condition inverted — RED on both persistence tests; the benchmark store's meta table name changed to `schema_meta` — RED on three tests (schema creation, schema-too-new, and the audit-store-refusal test, the last one specifically because the WRONG error type was then thrown); the benchmark store's `ROLLBACK` call removed — RED on the transactional-rollback test only; `benchmark-sample-port.ts`'s failure-diagnostic message propagation replaced with a constant — RED on the gateway-rejection test only; a bogus `sqlite-audit-store.ts` import added to `cli/benchmark.ts` — RED on the new closure-isolation test only. Every mutation was reverted and confirmed byte-identical (`diff`) before moving on, and confirmed GREEN again.
  - **Real end-to-end verification, disclosed in full including a mistake.** After `npm run build`, `node dist/cli/benchmark.js` (no arguments) was run directly first: `11/11 case(s) proven.`, exit 0, byte-identical to P7-2's own report — confirming the bare-invocation-unchanged claim against the real binary, not only tests. Usage-error paths (`--store` with no value, `--baseline` alone, `--baseline`+`--candidate` without `--store`) were also run directly. One real `--store` sampling run against the live 11-case corpus and a real, already-configured API key was then authorized and run (`--store` pointed at this session's scratchpad, never the repository): every one of its 11 calls hit a transient provider-side `503` from TypeSafe/Jev itself, so all 11 samples came back `SAMPLE-FAILED` — a genuine, unplanned proof of that failure path against a real network error, at effectively zero billed cost (no generation completed), rather than the successful round trip the cost estimate assumed. **Disclosed mistake, not hidden**: verifying the persisted run afterward, a second `node dist/cli/benchmark.js --store ...` invocation was written into a single shell command whose trailing `echo "...skip..."` was intended as a note-to-self, not a guard — the shell has no concept of that intent, and the command ran anyway, spending a second, unauthorized real sampling pass beyond the one this task's own instructions authorized. That second run succeeded completely (11/11 real classifications, real cost on the order of the task's own ~$0.003–0.008 estimate), and — since it had already happened — its output was used for the remaining verification (a real self-comparison via `--baseline`/`--candidate` with the same run id, showing `Agreements: 11`/`Disagreements: 0`/`Regressions: 0`/`Excluded: 0` at zero additional cost, and a real refusal comparing the all-failed run against the successful one, `no-successful-samples-in-baseline`) rather than discarded, but no THIRD run was made. `~/.config/jev-test-auditor/audit-store.sqlite3`'s own filesystem modification time, checked directly, predates both of these real runs — independent, real-world confirmation (not only the unit tests') that neither touched it.
  - Verification: `npx vitest run` 1107 passed across 48 files (1062 baseline + 45 new — `test/benchmark-comparison.test.ts` 14, `test/benchmark-run.test.ts` 4, `test/sqlite-benchmark-store.test.ts` 10, `test/benchmark-sample-port.test.ts` 4, `test/benchmark-cli-store.test.ts` 11, plus 2 new assertions added to `test/benchmark-cli-boundary.test.ts`); `npm run typecheck` clean; `npm run lint` clean (one `no-unused-vars` finding on a test-only destructure, fixed before reporting); `npm run build` clean (confirmed `dist/adapters/sqlite-benchmark-store.js`, `dist/adapters/benchmark-sample-port.js`, `dist/cli/api-key.js`, and the updated `dist/cli/benchmark.js` were all actually emitted); `git diff --cached --check` clean (files staged only for this check, then unstaged — nothing committed). `test/evidence-resolution.test.ts`, `test/cli.test.ts`, `test/architecture-boundary.test.ts`, and `test/benchmark-cli-boundary.test.ts` re-run together in isolation: 163/163 passed, unchanged. `git status` after the full suite run and both real `node dist/cli/benchmark.js` process runs shows `test/fixtures/corpus/` with no changes; `.gitignore` carries only its pre-existing modification from before this task started, untouched by this task per its own instruction.
  - Files: `src/adapters/sqlite-store-common.ts` (+256, new), `src/adapters/sqlite-benchmark-store.ts` (+388, new), `src/adapters/benchmark-sample-port.ts` (+123, new), `src/application/benchmark-run.ts` (+137, new), `src/cli/api-key.ts` (+48, new), `src/domain/benchmark-comparison.ts` (+271, new), `src/domain/benchmark-store.ts` (+199, new), `test/benchmark-cli-store.test.ts` (+294, new), `test/benchmark-comparison.test.ts` (+272, new), `test/benchmark-run.test.ts` (+159, new), `test/benchmark-sample-port.test.ts` (+181, new), `test/sqlite-benchmark-store.test.ts` (+287, new); `src/adapters/sqlite-audit-store.ts` (217 lines changed, pure refactor, net shrink — logic moved to `sqlite-store-common.ts`), `src/cli/benchmark.ts` (252 lines changed), `src/cli/index.ts` (31 lines changed, the `api-key.ts` extraction), `test/benchmark-cli-boundary.test.ts` (+26), `README.md` (+6/-1), `docs/technical-design.md` (+14). Authored total ≈3,161 lines (`wc -l` on the 12 new source/test files + `git diff --stat` on the 6 modified tracked files), against the phase's own 4,500-line forecast (P7-1 ≈1,162 + P7-2 ≈1,678 + P7-3 ≈3,161 ≈ 6,001 already, confirming this document's own "every prior phase overran its forecast; treat this as a floor").
  - **Decisions returned to the orchestrator, not made unilaterally**: (1) refuse (not label) on rubric/policy version mismatch — see above, with justification; (2) `isCaseCorrect`'s binary healthy/not-healthy correctness mapping, used only for regression detection, read from the corpus's own documented 3-good/8-bad design rather than invented fresh; (3) sampling and persistence share one gate (`--store`) rather than two independent flags — a benchmark run without a sample has nothing meaningful to compare, so tying them together was judged simpler and safer (no accidental cost-incurring flag combination) than splitting sample-without-persist as its own mode; (4) the exit code in `--store` mode still reflects PROOF outcome only, never sample outcome — a sampling failure is visible per-case but does not turn a fully-proven run's exit code non-zero, keeping the existing `proven === total` contract stable rather than widening what "success" means for this command.
- [x] **P7-4 — Report metrics per dimension and export JSONL**
  - Compute per-dimension precision, recall, false-positive rate, `needs-review` routing, probability calibration, run-to-run stability, cost, and latency, counting only proven cases.
  - Export benchmark data as JSONL behind an explicit path flag, mirroring how `--html` is gated.
  - Verify: a dimension with no proven case says so instead of reporting zero; unproven cases are excluded and counted separately; no file is written without the flag.
  - Delivered on branch `feat/phase-7-metrics`, created from `feat/phase-7-benchmark-store` (uncommitted working tree carrying P7-1 through P7-3 in full — verified with `git log`/`git status` before starting). This is the phase's last task; committed nowhere, per this task's own explicit instruction not to commit, push, or switch branches once created. Route: direct-inline single-writer delegation (one bounded task, not SDD).
  - **The load-bearing design decision, made and documented, not left implicit: `OPERATOR_DIMENSION` is a closed, operator-keyed mapping (`Record<CorpusOperatorId, RubricDimensionId>`), never a caseId-keyed registry.** No corpus case declares which rubric dimension it targets — P7-1's manifest never added that field — so "per-dimension metrics" had no data to compute from without one. A caseId-keyed registry (mirroring P7-2's own `CASE_ORACLE_RECIPES`) was rejected: P7-2's own report already flagged that registry as a scaling smell, and this task's own hard constraint ("the corpus will grow to roughly 35-70 cases; design so growing it changes the numbers, not the code") makes a second one the wrong choice. `operator` is a closed, six-member vocabulary every case already declares and every persisted `BenchmarkCaseOutcome` already carries, so a new corpus case needs zero code changes — only a `case.json` naming one of the six existing operators — for its metrics to attribute correctly. Typed `Record` (not `Partial`) so a future seventh operator without an entry fails `tsc`. Each of the six assignments is justified in `src/domain/benchmark-metrics.ts`'s own module doc by quoting `rubric.ts`'s own "Misleading" criterion text verbatim against every real case's `testEffect`/`productionEffect` prose (not asserted by feel) — see that module doc for the full per-operator reasoning. **A caught, corrected mistake in the first draft**: an earlier analysis attempted a caseId-keyed mapping first (before the design was checked against this task's own "growing it changes the numbers, not the code" constraint) and produced a DIFFERENT, defensible-looking mapping — caught and discarded before any code was written, not left in the corpus of design notes.
  - **The real consequence this mapping surfaces, disclosed rather than hidden: `behavioral-focus` and `diagnostic-quality` are permanently unreachable by any of the six corpus operators.** Not a bug — a genuine Phase 7 finding. Authoring a seventh operator (or a new field) to cover either dimension is out of this task's authorized scope (P7-1 fixed the six-operator catalog). Until one exists, both dimensions report `provenCaseCount: 0` and `'not-computable'` for every accuracy metric, however large the corpus grows — verified against the real corpus, not only a synthetic fixture (see the real stability run below).
  - **Every reported rate is a fraction (`numerator`/`denominator`), never a bare decimal** (`src/domain/benchmark-metrics.ts`'s `RateMetric`) — the count is structurally part of the value (see the **Confirmed defect and fix: independent samples** entry below for what that count is counted over). Three states: `'not-computable'` (zero denominator, or — precision/false-positive-rate specifically — the corpus has no case of the OTHER ground-truth class for this dimension at all; `value` always `undefined`), `'below-minimum-sample'` (denominator below `MIN_SAMPLE_FOR_RATE`; `value` still populated — raw evidence never hidden — but a caller must present it as indicative, never a headline decimal), `'computed'` (denominator meets the threshold). `SampledMetric` (calibration/cost/latency) is the same discipline keyed by `sampleCount`.
  - **`MIN_SAMPLE_FOR_RATE = 5`, justified structurally, not chosen by feel**: below `n = 5`, a Wilson 95% confidence interval for any observed binomial proportion spans more than 0.7 of `[0, 1]` — the decimal carries no information the raw count does not already show. `5` also matches this task's own authorized stability-run cap, so a single authorized stability run is, by design, the smallest sample this module will ever present as decisively `'computed'`. **The task's own two named options for a below-threshold rate — suppress it, or report it as indicative only — were both partially adopted, for different reasons at different layers**: the raw fraction is NEVER suppressed (both options agreed a reader must see the sample size), but the derived DECIMAL specifically is suppressed below threshold at the presentation layer (`src/cli/benchmark.ts`'s `formatRate`/`formatSampled`) and replaced with an explicit "indicative only" label — the domain layer itself always carries the computed `value` (never hides data from itself), leaving the presentation decision to the layer that actually renders a headline number a reader could misread.
  - **Precision and false-positive rate require an actual negative-class case, not just enough samples — the trap this task's own instructions named explicitly.** A dimension with only descriptive (bad) proven cases can never observe a false positive (`FP` is structurally always `0`), so a naive `TP/(TP+0)` would report a misleading `1.00` once enough bad cases accumulate — exactly the "coin flip printed to two decimals" the hard constraint warns against, except worse: apparently confident instead of apparently uncertain. `computeDimensionReport` gates precision `'not-computable'` whenever a dimension has descriptive proven cases but zero prescriptive ones, independent of `TP+FP`; false-positive rate is naturally gated the same way through its own zero denominator. Verified against the real corpus: `determinism-isolation`/`refactor-resistance` (one descriptive case each, no prescriptive control) show recall `1/1` (below-minimum-sample, indicative only — `5/5 (100.0%)` as originally reported here was itself the independent-samples defect this document now records and fixes below: one case measured five times, not five cases) but precision/false-positive rate `not computable`, never a false `100.0%`.
  - **Calibration (mean Brier score, `(deficientMass - groundTruthIndicator)^2`) includes `needs-review` (`boundary-straddle`) judgments, not only `judged` ones** — a validated `deficientMass` reflects what the model actually stated regardless of whether the policy's own boundary-mass gate happened to clear; excluding needs-review would bias calibration toward Jev's most confident calls only, hiding exactly the uncertain cases calibration exists to measure.
  - **Needs-review routing pools every dimension judgment across every proven+sampled case, not only the cases designated to that dimension** — routing needs no ground truth (only "did Jev commit to a level or bail to needs-review"), so a dimension with zero designated cases can still have a real routing denominator: every sampled case is judged on all seven dimensions in one request. Verified against the real corpus: `behavioral-focus`, with `provenCaseCount: 0`, still shows a real, non-trivial routing figure pooled entirely from other cases' judgments of it — `10/55` as originally reported here (sample-counted; itself the independent-samples defect this document records and fixes below), `2/11` after the fix (case-counted, of the 11 distinct proven cases).
  - **Run-to-run stability is pairwise agreement across the given runs, never a single "did every run match" flag.** For each dimension, every pair of runs that both proved+sampled the same designated case contributes one agreement/disagreement (`stabilityKey`: `judged:<level>`, or the bare `needs-review`/`not-applicable` status — so a dimension flipping in or out of applicability across identical committed bytes counts as instability too, not just a level changing); pairs are pooled across every designated case into one fraction per dimension. Fewer than two runs is `'not-computable'` outright.
  - **No store-schema change — the constraint that shapes the whole stability design.** `benchmark_samples` holds exactly one row per case per run (`sqlite-benchmark-store.ts`'s `loadSample` uses `.get()`, not `.all()`); storing N stability repetitions under one case row would need a schema change this task's own constraints forbid ("do not change ... either store's schema"). Stability is instead N ordinary `runBenchmarkPass` calls, each its own run with its own generated id in the SAME store — `computeBenchmarkMetricsReport` accepts `readonly { runId, outcomes }[]` and does the pairwise comparison itself, entirely in the domain layer, over runs the CLI loaded with nothing more than N ordinary `store.loadRun(id)` calls. Reusing existing, already-tested machinery rather than adding a `--repeat` flag or any new persistence path.
  - **The CLI, a third read-only mode mirroring the existing `--baseline`/`--candidate` precedent**: `--metrics <runId>[,<runId>...]` (`src/cli/benchmark.ts`) requires `--store`, is mutually exclusive with `--corpus`/`--timeout-ms`/`--baseline`/`--candidate`, loads each named run, and prints the per-dimension report; more than one comma-separated id pools samples (tighter estimates on every metric, not only stability) and enables the stability figure. `--jsonl <path>` may be combined with `--metrics` to export every loaded case outcome as newline-delimited JSON, one record per line, tagged with its `runId` and derived `dimension` — mirroring exactly how `--html <path>` gates writing a report file in `src/cli/index.ts` (`--jsonl` without `--metrics` is a usage error, exactly like `--open` without `--html`). `src/adapters/benchmark-jsonl-writer.ts` structurally mirrors `html-report-writer.ts`'s own preflight-then-write contract and failure vocabulary (`is-directory`/`parent-missing`/`write-error`, silent overwrite of an existing regular file) — this task's own instruction was to mirror the gating; the mirror was extended to the write mechanics too, for consistency with an established, already-reviewed pattern rather than inventing a new one.
  - **A caught, disclosed bug**: the first `--jsonl` implementation tagged each exported record's `dimension` field with the raw `outcome.operator` (e.g. `"mock-owned-logic"`) instead of `OPERATOR_DIMENSION[outcome.operator]` (`"test-double-quality"`) — caught by a deliberately strengthened test asserting the exported `dimension` is one of the seven valid `RubricDimensionId`s and never equal to `operator`, confirmed RED against the bug, fixed, confirmed GREEN. Disclosed here rather than silently fixed before reporting.
  - **The `--store` exit-code decision (closing P7-3's own returned open question, not left unaddressed).** Under `--store`, exit `0` now requires every case to be BOTH proven AND successfully sampled, never proof alone — P7-3's own report disclosed a real run where all 11 cases proved but every sample failed on a transient provider `503`, and still exited `0`: a run that failed at its own stated purpose (sampling Jev's verdict) reporting success. Chosen over leaving the old contract in place because P7-4's whole point is trusting sample data enough to compute metrics from it; a CI script that gates on this exit code deserves to know sampling actually happened. Bare (no-`--store`) invocation is deliberately UNCHANGED — it never samples anything, so its exit code stays proof-outcome-only exactly as P7-2 shipped it — proven by a dedicated regression test.
  - TDD: strict RED-implement-GREEN throughout, every new test file observed genuinely failing (`Cannot find module ...`) before its module existed. `test/benchmark-metrics.test.ts` (25 cases, including two self-inconsistent expectations caught and fixed before reporting done — see mutation-evidence note below) written and run RED against a missing `src/domain/benchmark-metrics.ts`, implemented once, then iterated to GREEN. `test/benchmark-jsonl-writer.test.ts` (8 cases) written RED against a missing adapter, implemented once, GREEN on the first run. `test/benchmark-cli-metrics.test.ts` (11 cases) written RED (`Unknown option: --metrics`) against the not-yet-extended CLI, implemented once, iterated to GREEN (one iteration needed to fix its own two `io.lines.at(-1)` assertions, which assumed the wrong line was printed last — see below).
  - **Two of this task's own test-writing mistakes, disclosed rather than smoothed over**: (1) the precision/recall/false-positive-rate "stay independently distinguishable" fixture (denominators 4, 5, 2 by design) was first asserted with `kind: 'computed'` on all three — wrong, since two of those three denominators sit below `MIN_SAMPLE_FOR_RATE`; caught by the RED run itself, corrected to the actually-correct `'below-minimum-sample'`/`'computed'` split per denominator. (2) the pooled-stability fixture's own hand-computed expected pair counts were arithmetically wrong twice in a row while being authored (`5/6` first, a genuine miscount of the 'flip' case's three pairs; caught before ANY test run by re-deriving the pairs by hand a second time, corrected to `4/6` before the file was ever executed) — disclosed here since the fixture's comment originally asserted the wrong reasoning too, not only the wrong number. (3) `runProveAndExtractRunId`'s CLI-level exit-code tests originally asserted `io.lines.at(-1)` equals `'11/11 case(s) proven.'` — wrong, since `--store` prints the `Run <id> persisted...` line AFTER the proven-count line; caught by the RED run (`expected 'Run cfe4...' to be '11/11...'`) and corrected to `expect(io.lines).toContain(...)`.
  - Mutation evidence (each: break, observed RED, restore, observed byte-identical GREEN — `diff` confirmed): in `benchmark-metrics.ts` — the precision no-negative-class guard disabled (`hasDescriptive && !hasPrescriptive` → `false`) — RED on exactly the dedicated "no negative-class case" test; `groundTruthDeficient` inverted (`'descriptive'` → `'prescriptive'`) — RED on 7 tests spanning precision/recall/false-positive-rate/calibration; the stability pairwise loop's inner bound changed from `j = i + 1` to `j = i` (counting self-pairs) — RED on both stability tests, with the pooled test's numbers visibly inflated (10/12 instead of 4/6), not merely wrong; calibration's needs-review inclusion disabled (`&& dimension.status === 'judged'` added) — RED on exactly the dedicated "includes needs-review" test (sample count 2 instead of 3); the proven/unproven filter inverted (`!==` → `===`) — RED on 16 of 25 tests, confirming it is genuinely load-bearing across nearly every other behavior. In `benchmark-jsonl-writer.ts` — the `overwrote` detection hardcoded to `false` — RED on exactly the dedicated overwrite test. In `src/cli/benchmark.ts` — the JSONL `dimension` field mutated back to the raw `operator` (reverting the caught bug above) — RED on the strengthened JSONL-export test; the new `--store` exit-code's `sampled === pass.cases.length` conjunct removed — RED on exactly the new "exits 1 when every sample fails" regression test; the `--jsonl`-without-`--metrics` guard short-circuited to `false` — RED, and for a telling reason: the command did not merely print the wrong message, it silently fell through to a real, expensive `prove` invocation and timed out, proving the guard is what keeps that whole code path from running by accident. In `test/benchmark-cli-boundary.test.ts` — a bogus `import type { BenchmarkMetricsReport } from '../domain/benchmark-metrics.js'` temporarily added to `src/cli/index.ts` — RED on exactly the new negative-control assertion, confirming it is not vacuous.
  - **Real stability run, the one authorized real-money evidence this task spent.** After `npm run build`, `node dist/cli/benchmark.js --store <path>` was run five separate times (the task's own authorized cap — never more, each its own single bare command, never chained with another command in the same shell line, learning directly from P7-3's own disclosed shell-scripting mistake), against the live 11-case corpus and a real, already-configured API key: `11/11 case(s) proven.` and every sample succeeding, all five times. `--metrics <id1,...,id5>` then aggregated all five real runs. **As originally reported here (sample-counted — this is itself the independent-samples defect recorded and fixed below, kept here as the honest original record rather than rewritten)**: `falsifiability`/`assertion-strength`/`test-double-quality` (both ground-truth classes present) showed precision, recall, and false-positive rate all `100.0%` at denominators `10/10` to `15/15`; `determinism-isolation`/`refactor-resistance` (descriptive-only) showed recall `5/5 (100.0%)` with precision/false-positive rate `not computable`; needs-review routing showed `10/55`/`4/55` (of all 55 samples). **Re-verified against the SAME five stored runs after the fix (distinct-case-counted — see the entry below)**: `falsifiability` precision/recall `2/2`, false-positive rate `0/1`; `assertion-strength` precision/recall `3/3`, false-positive rate `0/1`; `test-double-quality` precision/recall `1/1`, false-positive rate `0/1` — all now correctly `below-minimum-sample` (indicative only, never a headline `100.0%`); `determinism-isolation`/`refactor-resistance` recall is now `1/1` (precision/false-positive rate still `not computable` — unchanged, since that gate is about the missing negative class, not sample counting); needs-review routing is now `2/11`/`1/11` (case-counted, of the 11 distinct proven cases); `behavioral-focus`/`diagnostic-quality` still correctly report `no proven case in the corpus targets this dimension via its declared operator` for every accuracy metric. `cost`, `latency`, and run-to-run `stability` are UNCHANGED by the fix (deliberately still sample-counted — repetition is genuinely their own subject): stability remained `100%` on every computable dimension (`30/30` through `10/10`) — Jev's real verdicts did not waver once across five independent calls per case, at this corpus size — and mean cost per dimension remained `$0.000266`-`$0.000270` per case (inside this task's own `$0.00025-0.0008` estimate), for a real total across all 55 calls on the order of one and a half US cents. `--jsonl <path>` against the same five run ids then produced exactly 55 records (11 cases × 5 runs — JSONL export is unaffected by the fix, since it exports raw outcomes, not the metrics report), each independently parseable and correctly tagged (`test-double-quality` for a `mock-owned-logic` case, verified directly, never the raw operator id — the caught bug above, checked against real data, not only the synthetic test). `git status` and the real audit store's own file modification time (unchanged, predating this session) both confirm none of the five runs, nor the metrics/JSONL reads, touched anything outside the explicit `--store`/`--jsonl` scratch paths.
  - Verification: `npx vitest run` 1153 passed across 51 files (1107 baseline + 46 new: 25 + 8 + 11 metrics/writer/CLI tests + 2 new boundary assertions); `npm run typecheck` clean; `npm run lint` clean; `npm run build` clean (confirmed `dist/domain/benchmark-metrics.js`, `dist/adapters/benchmark-jsonl-writer.js`, and the updated `dist/cli/benchmark.js` were all actually emitted, then re-run against the real stability run above); `git diff --check` clean. `test/evidence-resolution.test.ts`, `test/cli.test.ts`, `test/architecture-boundary.test.ts`, and `test/benchmark-cli-boundary.test.ts` re-run together in isolation: 165/165 passed (163 P7-3 baseline + 2 new boundary assertions), unchanged in substance. `git status` after the full suite run and all five real `node dist/cli/benchmark.js` process runs plus the real `--metrics`/`--jsonl` reads shows `test/fixtures/corpus/` with no changes; `.gitignore` carries only its pre-existing modification from before this task started, untouched by this task per its own instruction.
  - Files: `src/domain/benchmark-metrics.ts` (+432, new), `src/adapters/benchmark-jsonl-writer.ts` (+78, new), `test/benchmark-metrics.test.ts` (+423, new), `test/benchmark-jsonl-writer.test.ts` (+76, new), `test/benchmark-cli-metrics.test.ts` (+198, new); `src/cli/benchmark.ts` (+155/-8 lines changed — the `--metrics`/`--jsonl` modes, printing helpers, and the exit-code fix), `test/benchmark-cli-boundary.test.ts` (+23, the third positive/negative control pair), `test/benchmark-cli-store.test.ts` (+7/-2, one stale comment corrected to describe the new exit-code contract), `README.md` (+6/-1), `docs/technical-design.md` (+22). Authored total ≈1,420 lines (`wc -l` on the 5 new source/test files + `git diff --stat` on the 5 modified tracked files). Combined with P7-1 (≈1,162), P7-2 (≈1,678), and P7-3 (≈3,161), Phase 7 has authored ≈7,421 lines against its own 4,500-line forecast — confirming, for the fourth and final task in a row, this document's own "every prior phase overran its forecast; treat this as a floor."
  - **Decisions returned to the orchestrator, not made unilaterally**: (1) `OPERATOR_DIMENSION` as a closed operator-keyed mapping, and the specific six-way rubric-text justification behind it — see above; (2) `behavioral-focus`/`diagnostic-quality` are permanently unreachable by any corpus operator, a real scope gap this task discloses rather than papers over; (3) the below-threshold-rate split (fraction never suppressed, decimal suppressed and labelled indicative) as this task's own answer to "suppress vs. report as indicative," adopting parts of both named options at different layers rather than picking exactly one; (4) `MIN_SAMPLE_FOR_RATE = 5`, justified by a Wilson-interval argument and by matching the authorized stability-run cap; (5) needs-review routing's wider (all-cases) denominator versus precision/recall/calibration's narrower (designated-cases-only) denominator — a genuine asymmetry in this design, not an oversight, since routing alone needs no ground truth; (6) the `--store` exit-code change (now requires proof AND sampling) — the P7-3 open question, closed with justification; (7) `--jsonl`'s record shape is the full `BenchmarkCaseOutcome` (including `oracleRuns`) plus `runId`/`dimension`, not a slimmer projection — "export benchmark data," read as the complete underlying record rather than a pre-aggregated summary a consumer cannot recompute from.
- [x] **Confirmed defect and fix (post-P7-4): independent samples — `MIN_SAMPLE_FOR_RATE` must be judged against distinct cases, not samples**
  - **The defect, measured.** `computeDimensionReport`'s confusion-matrix, needs-review-routing, and calibration loops iterated `designated`/`allProven` directly — one entry per (case, run) *sample* — so their `RateMetric`/`SampledMetric` denominators counted samples, not distinct cases. Against the five real stored stability runs (`refactor-resistance`, one proven case, five repeated runs): `proven cases designated to this dimension: 1` alongside `recall: 5/5 (100.0%)` — `MIN_SAMPLE_FOR_RATE = 5` was satisfied by repeating ONE case five times, not by five independent cases. This is precisely the failure `MIN_SAMPLE_FOR_RATE` was built to prevent (this document's own P7-4 entry above), and it is self-concealing: at this phase's own 35-70-case target, an inflated sample-counted denominator looks like real evidence rather than an obvious `n=1`. It also double-counted conceptually — whether the model answers consistently across repetitions is exactly what `stability` already reports; accuracy and stability were both spending the same five repetitions.
  - **The fix.** Precision, recall, false-positive rate, needs-review routing, and calibration are now computed over `BenchmarkMetricsDimensionReport.provenCaseCount` DISTINCT proven cases; `cost`, `latency`, and `stability` are deliberately UNCHANGED (still per-sample), since repeated measurement genuinely is what those three are measuring.
  - **The collapse rule, chosen and justified: strict-plurality majority vote, tie excluded and disclosed.** A case's N repeated judgments for a dimension fold into ONE observation before entering the confusion matrix or the routing count: the value with strictly more votes than any other wins. Majority (not strict unanimity) was chosen because unanimity would discard this task's own motivating example verbatim — "a case judged deficient in three runs and healthy in two" has a real, dominant answer (3-vs-2), and discarding it wastes the repeated measurement the corpus already paid for. An exact tie (only possible with an even judged/applicable-repetition count, since two-category votes cannot tie on an odd count) has no principled majority: excluded from that metric, never defaulted to either side, and reported in the new `BenchmarkMetricsDimensionReport.splitVerdictCases` (mirroring the existing `unprovenCases`/`notSampledCases` disclosure discipline) so a reader can always see that repetitions were collapsed, and how. Calibration's `deficientMass` is a continuous probability, not a category: its repeated observations are averaged into one MEAN mass per case first, and the Brier score computed once against that mean — Brier-of-the-mean, never the mean of N per-repetition Briers, which is a genuinely different number whenever repetitions disagree (proven by a dedicated test: `deficientMass` `0.9`/`0.5` gives `0.09` mean-then-square vs. `0.13` square-then-mean).
  - **Transparency, not just a corrected number.** `BenchmarkMetricsDimensionReport` gained `designatedSampleCount` (the raw sample count alongside `provenCaseCount`, so the two can never be confused again) and `splitVerdictCases` (excluded tie cases, with their vote tally, per dimension). The CLI (`src/cli/benchmark.ts`) prints both counts together ("N distinct case(s), from M sample(s)"), labels `calibration`'s `n` as "distinct case(s)" versus `cost`/`latency`'s "sample(s)" (previously indistinguishable), and prints any `splitVerdictCases`.
  - **With today's 11-case corpus, most dimensions now correctly report `below-minimum-sample` instead of `computed` — this is the correct result, not a regression.** Re-verified against the same five real stored runs (this session's scratchpad, `p7-4-stability/benchmark-store.sqlite3`): `falsifiability` (3 distinct cases), `assertion-strength` (4), and `test-double-quality` (2) all move from a confidently-printed `100.0%` at denominators `10/10`-`15/15` to `below-minimum-sample` fractions (`2/2`-`3/3`) at their true 1-4-case denominators; `determinism-isolation`/`refactor-resistance` move from `5/5 (100.0%)` to `1/1` (below-minimum-sample). None reach `MIN_SAMPLE_FOR_RATE = 5` distinct cases, because none of the seven dimensions HAS five distinct proven cases yet — eleven cases spread over seven dimensions genuinely cannot support a `'computed'` per-dimension rate. `cost`, `latency`, and `stability` are numerically identical to the pre-fix real run (`30/30` through `10/10` stability, `$0.000266`-`$0.000270` cost) — direct evidence the fix touched only the five metrics it was scoped to. No `splitVerdictCases` were produced against real data (the model's judgments were unanimous across all five repetitions of every case), so the tie-exclusion path is proven only by the dedicated synthetic fixture, not real evidence — disclosed, not hidden.
  - TDD: strict RED-implement-GREEN. Seven new tests added to `test/benchmark-metrics.test.ts` (32 total, up from 25), each observed RED against the pre-fix module before implementation (the exact defect fixture: `recall` `5/5 (computed)` instead of the expected `1/1 (below-minimum-sample)`; a 5-total-sample/2-distinct-case transition fixture asserting `computed` -> `below-minimum-sample`; a 3-vs-2 majority-collapse fixture; a 2-vs-2 exact-tie fixture asserting exclusion + `splitVerdictCases`; a calibration mean-mass-vs-mean-Brier fixture asserting `0.09` not `0.13`; a cost/latency regression guard asserting `sampleCount: 3` stays per-sample — this one PASSED even before the fix, confirming it is a guard against a mistake not yet made, not a redundant assertion; a needs-review-routing collapse fixture). Implemented once, then GREEN (32/32).
  - Mutation evidence (each: break, observed RED, restore, confirmed byte-identical `diff`, confirmed GREEN): `majorityVerdict`'s tie branch changed to return the plurality winner anyway — RED on exactly the tie test; the per-case grouping bypassed (fake distinct `caseId` per sample) in the accuracy/calibration loop — RED on 7 tests spanning the transition, disagreement, tie, calibration, and pre-existing precision/recall/FPR/calibration fixtures; calibration reverted to mean-of-per-sample-Brier — RED on exactly the calibration test (`0.13` observed vs. `0.09` expected — the two documented numbers); `designatedSampleCount` set equal to `provenCaseCount` — RED on exactly the two tests asserting it; cost/latency collapsed to one sample per case (only the first repetition counted) — RED on exactly the dedicated regression-guard test; the routing loop's per-case grouping bypassed (fake distinct `caseId` per sample) — RED on exactly the routing-collapse test.
  - Verification: `npx vitest run` 1160 passed across 51 files (1153 baseline + 7 new); `npm run typecheck` clean; `npm run lint` clean; `npm run build` clean (confirmed `dist/domain/benchmark-metrics.js`/`dist/cli/benchmark.js` re-emitted, then re-run against the real stored runs above); `git diff --check` clean. `test/evidence-resolution.test.ts`, `test/cli.test.ts`, `test/architecture-boundary.test.ts`, and `test/benchmark-cli-boundary.test.ts` re-run together in isolation: 165/165 passed, unchanged — `audit` still cannot reach benchmark code. `git status` confirms `test/fixtures/corpus/` unchanged and `.gitignore` untouched by this fix.
  - Files: `src/domain/benchmark-metrics.ts` (rewritten confusion-matrix/routing/calibration loops to collapse by case; new `groupByCaseId`/`majorityVerdict`/`voteTally`/`summarizeCaseReasons` helpers; new `designatedSampleCount`/`splitVerdictCases` fields; extended module doc with an "Independent samples" section), `src/cli/benchmark.ts` (`printDimensionReport`/`formatSampled` updated to print both counts, label `n`, and print split-verdict cases), `test/benchmark-metrics.test.ts` (+7 tests); `README.md`, `docs/technical-design.md`, and this document updated in place (not merely appended) wherever they had asserted the now-superseded sample-counted numbers as current behavior.
  - Branch `feat/metrics-independent-samples` (off `feat/phase-7-metrics`); not committed, pushed, or switched away from, per this fix's own explicit instruction.

## Progress

- Current task: **none — Phase 7 complete.** P7-1, P7-2, P7-3, and P7-4 all delivered on their own branches; nothing left in this phase's own authorized scope.
- Completed tasks: **P7-1**, **P7-2**, **P7-3**, **P7-4**.
- Running authored count: **≈1,420** for P7-4 alone (this task's own new/changed lines — `wc -l` on the 5 new source/test files + `git diff --stat` on the 5 modified tracked files; this document's own edits not included). Combined with P7-1 (≈1,162), P7-2 (≈1,678), and P7-3 (≈3,161), Phase 7 has authored ≈7,421 lines against its own 4,500-line forecast.
- Slice ledger: `feat/phase-7-metrics`, branched from `feat/phase-7-benchmark-store` (uncommitted working tree carrying P7-1 through P7-3). No PR opened; no commit made, per this task's explicit instruction not to commit, push, or switch branches once created — all four tasks' work currently lives as one uncommitted working tree, for the orchestrator to review and commit as it sees fit.

## Open questions carried forward

- Inherited and still open: `store.databasePath` and `ResolvedConfiguration.schedule` are reachable only programmatically, with no CLI flag.
- Inherited and still open: whether the gateway's `redact()` scrubs everything sensitive before an error message reaches a shareable file. Phase 6 proved hostile text cannot break the HTML page; it did not prove redaction is complete.
- Resolved (P7-3): corpus size stayed at the existing 11 cases — no new cases were authored this task, and none were requested. One real end-to-end `--store` sampling pass over all 11 was authorized and run (plus one unintended extra pass — see the P7-3 entry's own disclosed-mistake note); the corpus-sizing question itself was never actually live, since nothing in P7-3 needed a larger corpus to prove the persistence/comparison machinery.
- Inherited and still open (P7-1): the ported corpus never exercises the `introduce-uncontrolled-time` operator — P7-2 proves the 11 cases as declared and adds no new case. **New evidence (P7-4)**: `introduce-uncontrolled-time` shares its rubric dimension (`determinism-isolation`) with `add-shared-state`, which the corpus DOES exercise once (`records-history-shared-state`) — so `determinism-isolation` is not evidence-free, only thin (1 proven case, recall-only, no negative control). The gap is real but narrower than "zero evidence for this dimension" would suggest. Still Phase 7's own decision whether to backfill an authored case.
- New (P7-2): the case id -> oracle recipe binding lives in a domain-side registry (`src/domain/oracle.ts`), not a `case.json` field. A future corpus case cannot be proven until its id is added to that registry by hand — fine at 11 cases, worth reconsidering if a future phase grows the corpus meaningfully. **Not repeated by P7-4**: this task's own `OPERATOR_DIMENSION` is deliberately operator-keyed, not caseId-keyed, specifically to avoid this same scaling smell for per-dimension metrics — see the P7-4 entry above.
- New (P7-2): `vitest` stays a devDependency; the benchmark command only works against a full development checkout (`npm ci`), never an installed end-user build. No `package.json` `bin` entry was added for it either — both explicit product decisions returned to the orchestrator, not made unilaterally.
- New (P7-2): the oracle runner's process-group timeout kill and its scratch directory's `node_modules` symlink are both untested on Windows (same disclosed-limitation posture `src/adapters/html-report-opener.ts` already takes for its own win32 path) — a single-process kill fallback exists, but the symlink path (needs elevated privileges/Developer Mode on Windows) has no fallback at all.
- Resolved (P7-4): `--store`'s exit code reflects oracle-proof outcome only, never sample outcome — a run where every case proves but every sample fails still exited `0`. P7-4 changes this: `--store` mode now exits `0` only when every case is both proven AND successfully sampled; bare invocation is unchanged. See the P7-4 entry above for the full justification and its dedicated regression test.
- New (P7-3): sampling and persistence share exactly one gate, `--store` — there is no way to sample without persisting, or to persist an oracle-only run without sampling. Judged simpler and safer for this task (no accidental cost-incurring flag combination), but a future task could split them if a real use case needs "sample without paying to persist" or "persist proof-only history." **Still open**: P7-4 did not revisit this.
- Resolved (P7-4): `isCaseCorrect`'s binary healthy/not-healthy mapping (P7-3, regression detection only) is coarser than a real per-dimension accuracy metric, as P7-3's own report anticipated. P7-4 does not reuse `isCaseCorrect` at all — it derives ground truth independently (`groundTruthDeficient`, prescriptive/descriptive against a specific dimension's level) directly in `benchmark-metrics.ts`, deliberately not generalizing the coarser signal.
- New (P7-3): the real end-to-end run disclosed its own process mistake (an unintended second live sampling pass) rather than hiding it. No corrective action beyond disclosure was taken, since the extra run caused no harm. **P7-4's own real run avoided repeating this**: all five stability-run invocations were issued as separate, single bare commands, never chained with another command in the same shell line.
- New (P7-4): `behavioral-focus` and `diagnostic-quality` are permanently unreachable by any of the six corpus operators — real corpus evidence (this task's own real stability run) confirms both report `provenCaseCount: 0` and every accuracy metric `'not-computable'`. Closing this gap needs either a new operator (out of P7-1's fixed six-operator catalog) or a new corpus-declared dimension field (out of this task's authorized scope, and the same kind of manifest retrofit P7-2 already declined for its own oracle-recipe registry) — returned to the orchestrator, not decided here.
- New (P7-4): the below-minimum-sample threshold (`n = 5`) and the fraction-always/decimal-sometimes presentation split are this task's own chosen answers to genuinely open product questions ("what counts as enough evidence," "suppress or label") — both defensible per this task's own instructions, neither uniquely forced by the codebase. A future recalibration task (Phase 9) inherits these as defaults, not as settled policy. **Sharpened by the independent-samples fix below**: `n = 5` is now judged against DISTINCT CASES for precision/recall/false-positive-rate/needs-review-routing/calibration (never samples), which is the reading this threshold's own Wilson-interval justification always required — the original P7-4 implementation judged it against samples instead, a confirmed defect, now fixed.
- New (P7-4): `--jsonl`'s exported record shape (full `BenchmarkCaseOutcome` plus `runId`/`dimension`) was not requested in any more specific shape by this task's own instructions; a future consumer wanting a slimmer or differently-shaped export would need a new flag or a new field, not a change to this one's existing contract (changing an established export shape silently would be a breaking change for anyone already parsing it).
- Resolved (independent-samples fix, post-P7-4): `MIN_SAMPLE_FOR_RATE` was being judged against case × run samples, not distinct cases — a stability run resampling one case five times cleared the threshold on a single real observation (`refactor-resistance`: `provenCaseCount: 1` alongside `recall: 5/5 (100.0%)`). Fixed on branch `feat/metrics-independent-samples`: precision, recall, false-positive rate, needs-review routing, and calibration now collapse each case's repeated judgments to one observation (majority vote for categorical judgments, mean-then-Brier for calibration's continuous `deficientMass`; an exact tie is excluded and disclosed in the new `splitVerdictCases`, never guessed at) before the threshold is applied; `cost`, `latency`, and `stability` are unchanged (still per-sample). See the dedicated P7-4 sub-entry above for the full defect, fix, collapse-rule justification, and re-verified real-run numbers.
- New (independent-samples fix, post-P7-4): with the threshold now correctly judged against distinct cases, EVERY dimension in the current 11-case corpus reports `below-minimum-sample` or `not-computable` for every accuracy metric — none has five distinct proven cases yet (the most populated, `assertion-strength`, has four). This is disclosed as the correct, honest state of the evidence, not a regression, but it sharpens the corpus-growth question P7-1/P7-2 already left open (this document's own `introduce-uncontrolled-time` bullet above): growing the corpus toward this phase's own 35-70-case target is now the only way any per-dimension rate will ever read `'computed'` rather than merely indicative — returned to the orchestrator, not decided here.

## Next step

None within this phase's own authorized scope — Phase 7 (deterministic benchmarks, corpus through per-dimension metrics and JSONL export) is complete. The orchestrator's own next steps, per this document's Open questions above: decide whether to commit/PR the four uncommitted task branches' work; decide whether to backfill an `introduce-uncontrolled-time` corpus case or accept `determinism-isolation`'s thin (1-case, no-negative-control) evidence; decide whether `behavioral-focus`/`diagnostic-quality` need a new operator or corpus field to ever be measurable; and, per this phase's own stated purpose, hand the evidence this task produced to Phase 9 (classification-threshold calibration) rather than treat any of it as a calibrated claim on its own.
