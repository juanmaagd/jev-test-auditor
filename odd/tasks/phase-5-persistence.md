# Build Phase 5 persistence, caching, and resilient scheduling

## Objective

Make an audit run durable and repeatable: persist immutable runs, work items, attempts, judgments, and usage in SQLite; derive a complete content-addressed cache key so an unchanged test is never paid for twice; and replace the fixed bounded pool with checkpointed scheduling that throttles under provider pressure and resumes an interrupted run by its run id.

## Problem

Phase 4 delivered the first end-to-end MVP, but every result is in-memory only. `runBoundedPool` (`src/application/audit.ts:48`) dispatches evaluations with a fixed concurrency and returns `{ kind: 'success' | 'failure' }` outcomes that are never written anywhere, so a killed process loses the whole run and a re-run re-bills every test case. `hashEvidenceBundle` (`src/adapters/evidence-hash.ts:11`) exists with a doc comment reserving it for Phase 5 caching and has zero production call sites. The dry-run estimator counts every evaluable test as billable (`src/domain/estimate.ts:246`), which the technical design already acknowledges as provisional.

## Why

The first real run measured roughly 72k input tokens for 11 test cases. Without a cache, auditing a real repository repeatedly is both slow and paid for from scratch every time, and any interruption throws away completed judgments that are already immutable facts. Persistence is also the precondition for Phase 6 report manifests and Phase 7 benchmark history.

## Authorized scope

- Add a domain store port and a `node:sqlite` adapter with versioned, transactional migrations.
- Persist immutable runs, work items with terminal states, attempts, raw answers, normalized judgments, usage, and errors.
- Implement the complete content-addressed cache key over normalized test source, evidence bundle, rubric/questions, exact model id, and policy-relevant request options; add `--fresh`.
- Replace the fixed bounded pool with a scheduler that observes request/token budgets, reduces concurrency under provider throttling, restores it after successful windows, and commits each terminal work-item state immediately.
- Add `--resume <runId>` so an interrupted run continues without duplicating or losing completed work.
- Feed cache-hit and billable-request counts into the dry-run estimate.
- Update README and technical design for delivered behavior.
- Do not implement HTML reports, `--open`, report manifests beyond what persistence needs, benchmarks, operators, oracles, the benchmark-review skill, calibrated thresholds, or other languages.

## Scope and constraints

- SQLite driver is the built-in `node:sqlite` (user decision, 2026-09-20). It keeps the package at zero runtime dependencies, which is the posture `package.json` has today. `better-sqlite3` was considered and declined because it would add the project's first native dependency and a compile/prebuild step to a published CLI.
- `node:sqlite` was unflagged in Node **v22.13.0** (2025-01-07, commit `55239a48b6`, PR #55890), so `engines.node` moves from `>=22.12.0` to `>=22.13.0`. Verified against the Node 22.13.0 release notes, not from memory.
- The module is Stability 1.2 (release candidate) and emits an `ExperimentalWarning` on use. Suppress that one warning narrowly at the adapter boundary; never install a blanket warning filter that would hide unrelated Node warnings.
- Use `DatabaseSync` with prepared statements and `STRICT` tables. Statements are finalized; writes run inside transactions.
- Persistence is append-only. `--fresh` bypasses cache lookup but still writes a new immutable result; it never mutates or deletes a prior judgment.
- The cache key must include the rubric version. Rubric v2 rewrote the applicability questions, so a key that omits it would serve pre-v2 judgments for post-v2 questions. This is a required test, not a note.
- The gateway keeps its own per-call timeout, abort, and 429/529 backoff (`src/adapters/jev-http-gateway.ts:144`). Phase 5 scheduling wraps the outer dispatch loop in `runEvaluation`; it does not reimplement or bypass gateway retry.
- The store is opt-in and constructed lazily, following the `--evaluate` gateway pattern (`src/cli/index.ts:556`). An offline audit without evaluation must not create or open a database file.
- A corrupt or schema-incompatible store fails visibly with a named error; it is never silently recreated or migrated backwards.
- Never execute audited code. The database lives outside the audited repository's source tree and never contains the API key.
- Artifacts use English. Preserve unrelated untracked `.atl/` files. Conventional Commits without AI attribution.

## Decisions

- Cache key basis: `sha256` over a canonical serialization that already exists — `canonicalizeJevRequest` (`src/domain/jev-request.ts:369`) deterministically serializes `{ state, model, questions }`, where state derives from the evidence bundle and test case, model is the pinned `jev-1.13.0`, and questions come from `RUBRIC_V2`. The key adds the normalized full test source hash, the rubric version, and the classification policy version on top of that serialization, so a policy or rubric change invalidates cleanly.
- Work-item states are the design's seven: `pending`, `running`, `completed`, `cached`, `uncertain`, `skipped`, `failed`. Only `completed` and valid `cached` judgments participate in quality classification.
- Store port lives in `src/domain/audit.ts` beside `AuditEvidencePort` and `AuditEvaluationPort`, as an optional `store?:` field on `AuditPorts`, wired in `createProductionPorts` (`src/cli/index.ts:142`). The domain never imports the adapter.
- Adaptive throttling is derived from observed provider responses (429/529 and `retry-after`), not from a wall-clock heuristic, and never exceeds the configured `concurrency` ceiling.
- `--resume <runId>` parses like `--rootDir` (consumes the next argument); `--fresh` parses like `--evaluate` (boolean). Both are rejected without `--evaluate`.
- Database location defaults under the per-user config home already used by auth storage, not inside the audited repository, and is overridable by configuration.

## Delivery

- Strategy: `auto-chain`.
- Forecast: approximately 3,000 authored changed lines, generated files excluded. Phase 3 overran roughly fourfold and Phase 4 overran roughly 3.6x (7,267 against 2,000), so treat this as a floor, not a budget.
- Chain strategy: cached `feature-branch-chain`.
- Tracker boundary: `feat/phase-5-persistence`, based on `main` at `6e76d25`.
- Planned local child slices: store and migrations, cache key and `--fresh`, resilient scheduling, resume, cache-aware estimation.
- Remote tracker/child pull requests: not created; push and PR creation remain unauthorized remote operations.
- RDD: disabled/unmanaged (`gentle-ai review mode status`, decided by global, read 2026-09-20).
- TDD: enabled by explicit user confirmation (carried forward); require observed RED, GREEN, REFACTOR, and critical mutation evidence.
- Test runner: Vitest.

## Primary references

- `node:sqlite` API: <https://nodejs.org/api/sqlite.html>
- Node 22.13.0 release notes (sqlite unflag): <https://nodejs.org/en/blog/release/v22.13.0>
- Technical design, "Persistence and cache" and "Scheduling and recovery": `docs/technical-design.md:165`
- Implementation plan, work unit 5: `docs/implementation-plan.md:55`

## Acceptance criteria

Each is demonstrably met, per the evidence cited; none is marked met on assumption alone.

- Migrations create the schema from empty, are idempotent on re-open, run in a transaction, and a store at an unknown or newer schema version fails with a named, visible error. **Met** — P5-1 evidence (`createSqliteAuditStore migrations`/`native failure wrapping` describe blocks, `test/sqlite-audit-store.test.ts`).
- Two consecutive evaluations of an unchanged repository issue provider requests only on the first; the second reports every test case as `cached` with the same judgments. **Met** — P5-2 evidence (`test/audit.test.ts`'s "content-addressed caching" block, `test/cli.test.ts`'s "SQLite audit store" block).
- Changing the rubric version, the model id, the classification policy version, the test source, or any evidence fragment invalidates the key and forces a new request. Each of those five inputs has its own test. **Met** — P5-2 evidence (`test/cache-key.test.ts`, 5 dedicated invalidation tests plus a 6th rootDir-stability test).
- `--fresh` issues new requests despite a warm cache and appends a new immutable result without altering the prior one. **Met** — P5-2 evidence (`test/audit.test.ts`'s "--fresh bypasses lookup..." test; `test/cli.test.ts`'s "--fresh on a third run bypasses that reuse" test).
- An evaluation interrupted mid-run leaves every already-terminal work item committed; `--resume <runId>` completes only the outstanding items and duplicates none. **Met** — P5-4 evidence (`test/cli.test.ts`'s "resume wiring" central test, hanging-request technique; `test/resume.test.ts`; `test/resume-root-dir-identity.test.ts`).
- Provider throttling lowers effective concurrency and a clean window restores it, never above the configured ceiling. **Met** — P5-3 evidence (`test/scheduler.test.ts`; `test/audit.test.ts`'s "adaptive scheduling" block).
- `audit --dry-run` reports cache hits and billable requests separately, and its billable count matches what a subsequent real run actually issues. **Met** — P5-5 evidence above: `estimateDryRun`'s `cacheHits`/`initialCalls` fields, and `test/cli.test.ts`'s "THE central acceptance test," which counts real evaluation-port dispatches rather than comparing two derived numbers.
- An offline audit without `--evaluate` creates no database file. **Met and preserved.** P5-1's original test still passes unchanged; P5-5 extends the same guarantee to `--dry-run` (never *creates*, though it may now *read* an already-existing store — see P5-5 evidence above and "Audit store" in `README.md`), verified by a dedicated cold-dry-run test asserting no file or config directory appears.
- No `ExperimentalWarning` reaches the CLI output, and unrelated Node warnings still do. **Met** — P5-1's fresh-child-process proof (`test/bin-smoke.test.ts`) for the suppression mechanism itself, immune to Node's once-per-process dedup. P5-5's cold `--dry-run` path is structurally exempt rather than separately observed: `openSqliteAuditStoreForLookup` never calls `loadSqliteModule()` at all when the file is missing (mutation-tested at the adapter level — removing that early return turns a missing-file open into a raw native failure); an in-process `process.emitWarning` spy was tried and found vacuous (this test file's own top-level `import { DatabaseSync } from 'node:sqlite'` already fires and dedupes the warning before any test body runs), confirmed by forcing the load anyway and watching the spy loop stay green regardless — removed rather than left in as false evidence.

## Tasks

- [x] **P5-1 — Persist runs and judgments in SQLite behind a port**
  - Define the store port in the domain, implement the `node:sqlite` adapter with versioned transactional migrations, and wire it lazily from the CLI.
  - Persist runs, work items with terminal states, attempts, raw answers, normalized judgments, usage, and errors as append-only records.
  - Bump `engines.node` to `>=22.13.0` and suppress only the sqlite `ExperimentalWarning`.
  - Verify: migration from empty, idempotent re-open, incompatible-version failure, transaction rollback on a mid-write error, no database file without `--evaluate`, and that no secret is ever stored.
  - Evidence: `8330fe7` (`feat: persist audit runs and judgments in sqlite`) and `5934ab7` (`fix: reject foreign stores and cover persisted columns`) on `feat/phase-5-sqlite-store`; 14 files, 1,827 authored lines. Suite 25 files/709 tests (667 on `main` before the phase), typecheck, build, lint, and diff check passed. Schema version 1 creates `runs`, `work_items`, `attempts`, `judgments`, `errors`, `skips`, and an explicit `schema_meta` table — chosen over `PRAGMA user_version` so a foreign database reusing that pragma cannot be misread as ours. `node:sqlite` is loaded through a dynamic import inside a narrow suppression window, because a static import would emit the `ExperimentalWarning` before this module's own code could wrap it. `AuditEvaluationPort.evaluate` was widened to return `{ evaluation, classification }` because persisting raw answers, which the scope requires as distinct from normalized judgments, needs the raw `JevEvaluation`. Store construction is gated on a successful evaluation-port build, not merely on `--evaluate`, so a missing API key never creates a database file. `finishRun` performs the adapter's only row mutation, setting the run's own `finished_at`; it never rewrites a recorded fact about a test case.
  - Independent verification found and fixed five confirmed defects, all originally green at 697 tests: persisted columns were swap-blind because the fixtures used identical values on both sides (`jev-1.13.0` for both model fields, `2` for both version fields), so swapping `requested_model`/`responded_model`, `policy_version`/`rubric_version`, or `attempts`/`output_tokens` changed nothing — the last of these would have silently corrupted the P5-2 cache key; a foreign SQLite database lacking `schema_meta` was silently adopted and had audit tables created inside it; raw `ERR_SQLITE_ERROR` escaped for a non-database file, a directory, and a read-only file instead of a named domain error; the malformed `schema_version` branch was entirely uncovered, and deleting it left a string value resolving successfully with the migration loop silently skipped; and `withSqliteExperimentalWarningSuppressed` leaked a warning and permanently installed a stale wrapper under overlapping concurrent calls, now fixed with a shared patch and depth counter.
  - Orchestrator spot checks, run independently of both workers: suite 709/709; the built CLI without `--evaluate` emitted no `ExperimentalWarning` and created no database file or config directory; the compiled adapter was driven by hand against real SQLite, confirming schema version 1, a persisted run and terminal work item, and a non-destructive idempotent reopen; the `policy_version`/`rubric_version` swap was re-applied by hand and turned RED (`expected 6 to be 3`); and a real foreign database, a garbage file, and a directory were each rejected with `AuditStoreCorruptError` while a genuinely empty file still migrated.
  - Reported honestly and accepted: the `migrate()` early-return guard at the top of the function is dead code no mutation can kill, since the loop bound already makes it a no-op, and the CLI's `evaluationPort !== undefined` guard before store construction is unreachable because the preceding catch already returns. Both are harmless and left in place as protection against a future refactor.
- [x] **P5-2 — Key, store, and reuse judgments by content**
  - Compose the complete cache key over normalized test source, evidence bundle, canonical request serialization, rubric version, model id, and classification policy version.
  - Look up before dispatch, record `cached` work items, and add `--fresh` as an append-only bypass.
  - Verify: warm-cache reuse, one invalidation test per key input, `--fresh` appends without mutating, and a pre-v2 rubric judgment never serves a v2 question set.
  - Evidence: `86d1416` (`feat: reuse judgments by content-addressed cache key`) on `feat/phase-5-cache-key`, off `feat/phase-5-sqlite-store`. 13 files touched (2 new: `src/adapters/cache-key.ts`, `test/cache-key.test.ts`); 935 insertions / 58 deletions across tracked files plus 308 lines of new files, ~1,301 authored lines against this task's own slice of the 3,000-line phase forecast. Suite 26 files/734 tests (709 baseline), typecheck, lint, build, and `git diff --check` all passed.
    - **Key composition** (`src/adapters/cache-key.ts`): `sha256` over `canonicalizeJevRequest(request)` (already covers the pinned model id via `request.model` and every rubric question's wording plus the evidence bundle's selected fragments/denied/unresolved/omitted entries via `request.state`) plus three inputs it does not cover — the normalized full test-file source hash, the rubric's numeric `version`, and the classification policy's numeric `version`. `hashEvidenceBundle` stays unused: its `canonicalizeEvidenceBundle` serialization additionally hashes span/contentHash/byte-counts, which would over-invalidate on a pure line shift.
    - **Store** (`src/adapters/sqlite-audit-store.ts`): schema version 2 adds a nullable, indexed `work_items.cache_key` column. `AuditStorePort.lookup(cacheKey)` returns the most recent (`work_items.id DESC`) `completed` judgment whose attempt's `model_matches_pin` is `1`; a `cached` row's own judgment is never itself a lookup source. A `completed` row with no cache key (pre-P5-2 caller) can never be a hit — `cache_key = ?` never matches `NULL`.
    - **Application** (`src/application/audit.ts`): `runEvaluation` looks up each evaluable item's key before dispatch when a cache-key port and store are both present; a hit records `cached` and reuses the judgment at zero token cost, a miss (or `--fresh`) dispatches and records `completed` with the key. `AuditEvaluationTotals` gained `cached`, counted in `statusCounts`/`respondedModel`/`modelMismatches` like `evaluated` but excluded from `usage` (a hit's classification carries the *original* spend, not a fresh one).
    - **`--fresh`** parses like `--evaluate` (boolean), rejected without `--evaluate`, mirroring the existing mutual-exclusion checks; it bypasses lookup only, never recording.
    - **`uncertain`**: documented, not implemented — nothing in this codebase produces it; the `CHECK` constraint and `WORK_ITEM_STATES` admit it for forward compatibility only.
    - All 6 required cache-key tests (5 invalidation + rootDir-stability) plus a 7th documented-scenario test live in `test/cache-key.test.ts`; the model-id and evidence-fragment invalidation tests prove the "already covered by `canonicalizeJevRequest`" claim rather than assert it. Every new behavior (lookup pin-skip/most-recent/no-key-never-hit, `--fresh` bypass, cached-totals exclusion from usage, `--fresh` CLI rejection, cache-key port pairing) was mutation-tested by hand: break, confirm RED, restore.
    - Orchestrator spot checks, run independently of the writer: suite 734/734 with typecheck, lint, build, and diff check clean; the rubric version was forced to a constant inside the key payload by hand and the suite turned RED (`expected 'a4dcec34…' not to be 'a4dcec34…'`), proving the version is load-bearing and that a pre-v2 judgment cannot serve a v2 question set; the schema v1-to-v2 upgrade path was confirmed covered by a hand-built v1 database rather than only by fresh creation, which alone could not distinguish "ran both migrations" from "created v2 directly"; and the production path was traced to confirm a `completed` row always carries its key, since the store exists only under `--evaluate` and the cache-key port is constructed together with it.
    - Honestly reported, not silently worked around: `lookup`'s explicit `w.state = 'completed'` predicate is not independently killable by any test that only goes through `recordWorkItem`, since a `cached`/`failed`/`skipped` work item never gets its own `attempts` row (only `insertAttempt` for `completed` does), so the `INNER JOIN` to `attempts` already excludes every non-completed row on its own. Added one test that manufactures the one case where the predicate matters (direct SQL inserting a non-completed row with a matching `attempts`/`judgments` pair, the same technique the P5-1 schema-corruption tests already use) so the predicate is genuinely mutation-killable rather than merely documented.
- [x] **P5-3 — Schedule evaluations with adaptive throttling and checkpoints**
  - Replace `runBoundedPool` in the evaluation path with a scheduler that commits each terminal work-item state immediately and observes request and token budgets.
  - Reduce concurrency on provider throttling and restore it after successful windows, never above the configured ceiling; keep gateway-level retry untouched.
  - Verify: a stubbed gateway that returns 429 lowers concurrency, a clean window restores it, and every terminal state is committed before the next item starts.
  - Evidence: `aec02f8` (`feat: schedule evaluations with adaptive throttling and checkpoints`) on `feat/phase-5-scheduler`, off `feat/phase-5-cache-key`. 16 files touched (3 new: `src/domain/scheduler.ts`, `src/application/scheduler.ts`, `test/scheduler.test.ts`); tracked-file diff 979 insertions / 113 deletions plus 585 lines of new files, ~1,677 authored lines against this task's own slice of the 3,000-line phase forecast (`.gitignore`'s pre-existing unstaged `M` excluded). Suite 27 files/777 tests (734 on this branch before this task), typecheck, lint, build, and `git diff --check` all passed, confirmed stable across 8+ consecutive full-suite runs.
  - A genuine regression was found and fixed during this task's own verification, not left for later: the new `pending`/`running` checkpoints roughly triple the store's transaction count per evaluated item, and SQLite's default rollback-journal mode does a full `fsync` per `COMMIT` — this made one pre-existing, unrelated CLI test (`test/cli.test.ts`, "uses the locally stored key...", which self-audits this repository's own ~700+ test cases against a real SQLite store) intermittently exceed its 5-second timeout under full-suite parallel load, though it passed reliably in isolation. Root-caused by measuring the same test's duration before/after this task's changes (1.76s → 3.04s) rather than assumed. Fixed by opening the store in WAL journal mode with `synchronous=NORMAL` (`src/adapters/sqlite-audit-store.ts`), which measured faster (1.65s) than the pre-P5-3 baseline despite 3x the writes. Moving the `PRAGMA` calls inside the existing native-failure try/catch (not naively placed before it) was itself required by a second regression this fix introduced and then fixed within the same pass: two existing tests (garbage file, read-only file) started receiving a raw native error instead of the named `AuditStoreCorruptError`, because setting WAL mode on an unwritable file fails immediately. Both the performance fix and its own regression are covered by new tests in `test/sqlite-audit-store.test.ts` (`createSqliteAuditStore write performance mode`), each mutation-tested (break, confirm RED, restore).
    - **Throttle signal** (`src/application/audit.ts`): reused the existing seam rather than widening the gateway. A successful evaluation's own `JevEvaluation.attempts > 1` is possible only because the gateway retries exclusively on 429/529 (`src/adapters/jev-http-gateway.ts`), so it is exact evidence of "the provider pushed back, then let this one through." A failed evaluation whose typed error kind is `'rate-limit'`/`'overloaded'` is throttling that was never recovered from. Every other outcome — a cache hit, or any other failure kind (auth, malformed response, timeout, abort) — reports `'neutral'`: it changes concurrency in neither direction and neither advances nor resets the clean-window streak, so an unrelated failure storm can never be mistaken for either direction. No gateway change was needed or made.
    - **Adaptive concurrency controller** (`src/domain/scheduler.ts`, pure domain — no I/O, no timers): starts at the `concurrency` ceiling, halves (floored, never below 1) on `'throttled'`, and restores by one step after `DEFAULT_ADAPTIVE_CONCURRENCY_RESTORE_WINDOW` (5, a named non-configurable constant) consecutive `'clean'` reports, never above the ceiling. `'neutral'` is a true no-op: it neither changes the limit nor advances/resets the streak.
    - **Dynamic dispatcher** (`src/application/scheduler.ts`'s `runAdaptiveSchedule`): replaces `runBoundedPool` entirely (removed). Re-reads `controller.limit` before every dispatch decision, so a throttle reduction or a clean-window restoration takes effect on the very next decision, never only at the start of the run; results still land at their original submission index regardless of completion order.
    - **Request/token budget gate** (`src/application/scheduler.ts`'s `createRequestTokenBudgetGate`): two independent rolling windows (60s requests, 1s tokens), defaulting to `JEV_VERIFIED_RATE_LIMITS` (1,200/min, 250k/s) via new `ResolvedConfiguration.schedule` (`src/domain/config.ts`, validated, overridable by a library consumer exactly like `store.databasePath` — no CLI flag). `waitForCapacity()` reserves one request slot atomically (synchronous check-then-push, no `await` in between) so two dispatches requested back-to-back cannot both slip past a stale check before either reports usage; a dispatch's real retried-attempt count and billed tokens are folded in afterward via `recordDispatch`, since token cost can never be known before the provider responds. `clock`/`sleep` are always injectable (`RunAuditOptions.clock`/`.sleep`), defaulting to the real clock/timer.
    - **Durable checkpoints** (`src/domain/audit.ts`'s `AuditStoreWorkItemOutcome` widened with `pending`/`running`, identity-only, no schema migration needed — schema version 2's `CHECK` constraint already admitted these seven states since P5-1): every evaluable item gets a `pending` row up front (before the scheduler dispatches anything), then `running` the moment it's picked up, then its terminal outcome; a `skipped` item skips straight to its terminal record (never dispatched). Append-only throughout.
    - **P5-2's `lookup` regression, verified not broken**: `state = 'completed'` stays correct once `pending`/`running` rows exist for the same item — confirmed with a realistic test (both checkpoints recorded via the real port alongside a real `completed` row) and a defensive direct-SQL test (a `running` row given a matching cache key and a matching `attempts`/`judgments` pair, mirroring P5-2's own `failed`-state defensive test) in `test/sqlite-audit-store.test.ts`.
    - Orchestrator spot checks, run independently of the writer, all against the compiled adapter rather than through the writer's test harness. Switching to WAL made P5-1's API-key canary test conditionally vacuous, which P5-1's own independent verifier had already named as a hypothetical risk before WAL existed: measured directly, a committed row is absent from the main database file while a 74 KB `-wal` sidecar holds it, and only appears in the main file after the last connection closes and checkpoints. The canary test scans only the main file, so it stayed meaningful purely because `runCli` closes the store — one refactor away from passing against an essentially empty file. Strengthened in this commit to assert the scanned bytes actually contain persisted content before trusting the key scan, and to hold any surviving `-wal`/`-shm` sidecar to the same guarantee; proved non-vacuous by removing the CLI's `storePort.close()` and observing RED (`expected 'SQLite format 3\u0000…' to contain 'work_items'`).
    - Orchestrator durability check, directly relevant to P5-4: a process was killed with `SIGKILL` mid-run without closing the store, leaving orphaned `-wal` and `-shm` files behind. Reopening recovered the committed work item intact, so WAL improves rather than weakens crash durability and the checkpoint rows P5-4 will read survive an abrupt kill. This was measured, not assumed.
    - Every important behavior was mutation-tested by hand (break, confirm RED, restore): throttle-from-attempts derivation, throttle-from-error-kind derivation (both directions), the halving formula, the restore-window boundary, the request-budget threshold, the extra-retried-requests folding, the `waitForCapacity` wiring itself, the `pending`/up-front and `running`/on-pickup recording, and the sqlite `lookup` state filter. Two neutral-vs-clean discriminations (a non-throttle failure, and a cache hit) initially had non-discriminating tests — caught only because an independent review pass re-ran them against a deliberately wrong implementation and found them still green; both were rewritten as genuinely discriminating tests (verified RED against the wrong implementation, GREEN against the correct one) before this report, not left as an unverified claim.
  - Independent verification found and fixed: none reported by an orchestrator-run separate verifier as of this writing — self-verification and mutation testing above were run by the same worker who implemented this task; an independent spot check is still open.
  - Honestly reported, not silently worked around: `--resume` (P5-4) is not implemented. What P5-4 can read: for a given `runId`, select every `work_items` row ordered by `id`, group by `(test_case_id, repository_relative_path, name)`, and take each group's last row — its `state` is `pending`/`running` (outstanding) or one of the four terminal states (done); `runs.finished_at IS NULL` already marks an interrupted run. P5-4 still needs its own work this task deliberately did not do: no `AuditStorePort` method exists yet to read a run's rows back; `runAudit` always calls `beginRun` (a fresh UUID), so resuming an existing `run_id` needs a way to continue rather than start; and the `pending` loop writes one sequential transaction per evaluable item before the first dispatch — fine at today's scale, a candidate for batching later.
- [x] **P5-4 — Resume an interrupted run by run id**
  - Add `--resume <runId>`, reload outstanding work items from the store, and complete only those.
  - Verify: a gateway fake that throws mid-run leaves completed work committed; resuming issues requests only for outstanding items, duplicates none, and produces the same final report as an uninterrupted run.
  - Evidence: `2ae8caf` (`feat: resume an interrupted run by run id`) and `7b80a8e` (`fix: identify a run by its canonical root directory`) on `feat/phase-5-resume`, off `feat/phase-5-scheduler` at `8729385`. 11 files touched (1 new: `test/resume.test.ts`); tracked-file diff 1,073 insertions / 38 deletions plus 442 lines of the new file, ~1,553 authored lines against this task's own slice of the 3,000-line phase forecast (`.gitignore`'s pre-existing unstaged `M` excluded, `.atl/` untouched). Suite 28 files/800 tests (777 on this branch before this task), typecheck, lint, build, and `git diff --check` all passed, confirmed stable across 3 consecutive full-suite runs plus 5 additional runs of the new resume-only tests.
    - **Store read method** (`src/adapters/sqlite-audit-store.ts`, `src/domain/audit.ts`): `AuditStorePort.loadRunState(runId)` — no schema change. Selects every `work_items` row for `runId`, groups by `(test_case_id, repository_relative_path, name)` via `MAX(id)` (mutation-tested against `MIN(id)`, confirmed RED), and reconstructs a full `AuditStoreWorkItemOutcome` from `attempts`/`judgments`/`errors`/`skips` for each identity whose last row is one of the four terminal states. A `pending`/`running` last row, or no row at all, is simply omitted — this port never decides what is "outstanding"; only `runAudit` does, since it alone knows the currently evaluable/skippable set. Also returns the run's own `rootDir` and `finished` (`finished_at IS NOT NULL`).
    - **Run-continuation mechanism**: no change to `beginRun`'s signature. `work_items.run_id` is a plain foreign key into an existing `runs` row, so resuming simply skips calling `beginRun` and reuses `options.resume` directly as the run id for every `recordWorkItem`/`finishRun` call — no schema migration, no new write method, and `finishRun` is naturally idempotent-safe to call again (it only ever sets `finished_at`, previously `NULL`).
    - **`--resume` + `--fresh` decision**: orthogonal, and the combination is fully defined and tested. `--resume` selects *which* items this invocation dispatches (only the outstanding ones); `--fresh` selects whether the items that *do* dispatch consult the cache first. No special-casing needed: `cache.fresh` threads into `runEvaluation` exactly as before, and is only ever consulted on the dispatch path an already-terminal reused item never reaches. Verified both ways in `test/resume.test.ts`'s "--resume combined with --fresh" describe block (an outstanding item served warm without `--fresh`, the same shape re-dispatched with `--fresh`, and an already-terminal item untouched either way, confirmed via `lookupCalls`).
    - **Named error cases** (`src/domain/audit.ts`, new `AuditResumeRunNotFoundError`/`AuditResumeRootDirMismatchError`/`AuditResumeUnavailableError`, caught by the CLI exactly like `AuditStoreSchemaVersionError`/`AuditStoreCorruptError`): a run id that does not exist, and a run id recorded against a different `--rootDir`, are both usage errors — named message, exit 1, no stack trace, verified end-to-end through the real `node:sqlite` adapter in `test/cli.test.ts`. A run that is already finished with nothing outstanding is explicitly NOT an error: `runAudit`'s `preflightResume` returns immediately (before any discovery at all) with `resume: { outstanding: 0, reused: 0, nothingOutstanding: true }`, and the CLI reports `Nothing to resume: run <runId> has no outstanding work items.`, exit 0. A resumed run whose items are ALL terminal but `finished_at` is still `NULL` (a crash between the last item and `finishRun` itself) reaches the same `nothingOutstanding: true` outcome through the general partition logic (outstanding count computed to be zero after a real discovery pass), not the early-preflight shortcut — both paths report identically to the caller.
    - **Re-billing decision (Decision 1)**: documented plainly, not engineered around. A `running` item's in-flight request may already have been billed before its answer was ever recorded; there is no signal available to distinguish that from "the request never reached the provider," so resuming always re-dispatches it. Disclosed in the CLI's `--resume` help text and `README.md`'s new "Resume" section, and in `docs/technical-design.md`'s "Scheduling and recovery".
    - **Completing only outstanding items** (`src/application/audit.ts`'s `runEvaluation`): every currently evaluable item is partitioned by identity against the resumed run's `terminalWorkItems`; a `completed`/`cached`/`failed` match is reused (reconstructed via `outcomeFromTerminal`, a reused `failed`'s diagnostic replayed through a small `ResumedEvaluationFailure` carrying the ORIGINAL `errorKind`/`errorMessage`) and merged back into `items`' own deterministic order; everything else is dispatched exactly as before, with its own fresh `pending` checkpoint (an honest additional append, never a duplicate of an earlier one for the same identity). A skipped item whose identity already carries a `skipped` terminal record is not re-recorded.
    - **Source-drift safety net**: a currently-evaluable item whose ONLY resumed-run match is a `skipped` terminal record (the source changed between the interrupted attempt and this resume — resume's documented assumption is an unchanged repository) is treated as outstanding, not reused — it was never actually evaluated by the provider before. Verified with a dedicated test and its own mutation (`isResumableTerminal` forced to accept every state — confirmed RED on exactly this test, and no other).
    - **Reporting**: `AuditResult.resume?: AuditResumeSummary` (`{ runId, outstanding, reused, nothingOutstanding }`), present only when `RunAuditOptions.resume` was used; `--evaluate --json` carries the same three fields under a `resume` key, the text report adds one summary line. Neither changes shape for an ordinary (non-resume) run.
    - **The task's central test** (`test/cli.test.ts`, "resume wiring"): a real `node:sqlite` store (not a fake), a fixture with two test cases, and a deterministic evaluation port whose `evaluate()` call for one test case returns a promise that never resolves at all (`new Promise<never>(() => {})`) — the closest a unit test gets to a literal SIGKILL: a genuinely in-flight, un-caught request, as opposed to a rejection, which P5-3's own per-item failure isolation always catches into a terminal `failed` outcome and can therefore never leave an item `pending`/`running` for `--resume` to find. Documented explicitly in the test and in this evidence: **a "gateway fake that throws" cannot simulate an interrupted run under this codebase's existing, deliberate per-item isolation** — this was verified, not assumed, by first trying the literal reading and confirming (via reasoning about the code, corroborated with the advisor) that every `evaluate()` rejection is unconditionally caught before this task's own code ever runs. The hanging-request technique is the honest substitute; the test polls the real store's `loadRunState` until the completed item genuinely commits, closes that connection (simulating a clean process restart against the same on-disk file, matching P5-3's own measured SIGKILL/reopen evidence), reopens a fresh connection for `--resume`, and asserts only the outstanding item is ever dispatched. A separate baseline run against an independent database (never touched by the interrupted/resumed pair, so a warm cache can never make the comparison vacuous) produces a `--evaluate --json` report compared, field for field, against the resumed run's own report with only the intentional `resume` key removed — proving "same final report as an uninterrupted run" directly rather than by inspection.
    - Every important new behavior was mutation-tested by hand (break, confirm RED, restore): the `MAX(id)`/`MIN(id)` grouping, the resumable-terminal-state filter (source-drift safety net), the outstanding/reused count computation (swapped, confirmed both affected assertions turn RED), the skip-dedup filter (removed, confirmed a duplicate `skipped` checkpoint appears), the `rootDir` mismatch check (inverted, confirmed 6 of 8 `test/resume.test.ts` cases turn RED), the `finished` gate (inverted, confirmed 5 cases turn RED), the CLI's `nothingOutstanding` branch condition, and the CLI's resume-error `instanceof` catch (narrowed to always-false, confirmed the affected test surfaces a raw uncaught exception instead of exit code 1 — proving the catch is load-bearing, not decorative).
    - Honestly reported, not silently worked around: this task's self-verification and mutation testing were run by the same worker who implemented it — no independent orchestrator spot check has been performed on this work as of this report, consistent with how P5-3 itself flagged the same gap for its own mutation-testing pass. `rootDir` is compared as a raw string with no path normalization (a pre-existing gap: neither `getResolvedConfiguration` nor `beginRun` ever resolves or canonicalizes it), so a legitimate resume against the same repository audited via two differently-spelled paths (relative vs. absolute, or a trailing slash) would be rejected as a root mismatch even though it is the same repository — not introduced by this task, and every test in this task deliberately uses one consistent `rootDir` string throughout to avoid masking that gap rather than accidentally depending on it. **Addendum, orchestrator review, 2026-09-20**: this gap was more serious than described here — the same raw-string comparison also produced a false ACCEPT across two different repositories that both default to `"."`, not only a false reject across spellings of the same one. Fixed before commit; see "Open questions carried forward" below for the full design, symlink, and backward-compatibility decisions.
  - Orchestrator spot checks for the rootDir-identity fix, run independently of the writer against the compiled adapter. The defect was first proved by calling `getResolvedConfiguration` from two different working directories, both returning `"."`, which made the resume guard compare equal across unrelated repositories — a silent cross-repository false accept, not merely the false reject the writer's own report had named. After the fix, the same probe run from two directories persists two distinct canonical absolute paths, and `realpath` demonstrably resolved macOS's `/var` to `/private/var`, so symlink resolution is load-bearing rather than decorative. A hand-inserted pre-fix run holding the raw `"."` reads back as `rootDirCanonical = false`, so it is refused with the named legacy error instead of being reinterpreted against the resume-time working directory, which would have reproduced the original bug.
  - The root cause of why this survived is worth recording: every one of P5-4's eight resume tests used a single consistent `rootDir` string. A test that never varies a value cannot detect that the value carries no meaning — the same family of defect as P5-1's symmetric column fixtures, in a different disguise. The writer also found, while fixing this, that P5-4's pre-existing mismatch test was passing on a macOS-coincidental substring match, and strengthened it to an exact-message assertion.
- [x] **P5-5 — Make the dry-run estimate cache-aware**
  - Feed per-test-case cache-hit lookups into `estimateDryRun` so `initialCalls`, estimated tokens, and estimated cost count only billable requests.
  - Verify: cold estimate unchanged from today, warm estimate reports hits and a reduced billable count, and the billable count equals the requests a following real run issues.
  - Evidence: implemented on `feat/phase-5-cache-aware-estimate`, off `feat/phase-5-resume` at `81fd24a`; not committed — left in the working tree per this task's own instructions. Tracked-file diff 1,005 insertions / 46 deletions across 11 files (no new files), ~1,051 authored lines against this task's own slice of the 3,000-line phase forecast. Suite 29 files/841 tests (815 on this branch before this task, +26 new), typecheck, lint, build, and `git diff --check` all passed, confirmed stable across 6 consecutive full-suite runs.
    - **The central design problem, resolved empirically, not assumed.** `--dry-run` is documented (`README.md`, `docs/technical-design.md`) as making no network call, requiring no API key, and writing nothing to disk; P5-1 additionally guarantees no database file or config directory without `--evaluate`. Reading the cache needs to open the store, which is not free by default: `createSqliteAuditStore` runs migrations (a write) and P5-3's WAL mode means even an ordinary `readOnly: true` open still creates `-wal`/`-shm` sidecar files on its first read (verified with a throwaway script under the scratchpad before writing any production code: a plain `{ readOnly: true }` open against a real WAL-mode store created a 33 KB `-shm` and a 0-byte `-wal` file on the very first `SELECT`, because SQLite still needs the wal-index for a consistent read even when it cannot write). The fix, found empirically and confirmed on this machine's real `node:sqlite`: open through a `file:` URI carrying `immutable=1`, plus `{ readOnly: true }` as a second, independent guard — this created zero sidecar files, left the main database file byte-identical (hash and size) both against a cleanly closed store and one with an uncheckpointed `-wal` file from a still-open writer connection, and a missing file failed cleanly (`unable to open database file`) with nothing created either way. `immutable=1` is the load-bearing piece, confirmed both ways rather than assumed: opening WITHOUT the `{ readOnly: true }` option at all (only `immutable=1` in the URI) still refused a write outright ("attempt to write a readonly database") and created no sidecar, since `immutable=1` alone already tells SQLite the file will not change; `{ readOnly: true }` is kept anyway as a second, `node:sqlite`-level guard, not because it is independently necessary. Building the URI with `pathToFileURL(...).href` (never raw string concatenation) was independently confirmed necessary: a `#` in a directory name (probed by constructing a fixture path containing one) silently truncated a hand-built `file:` string at the `#`, while `pathToFileURL` percent-encoded it correctly and opened the real file.
    - **The one empirically-confirmed limitation of `immutable=1`, disclosed rather than hidden**: a row sitting only in an uncheckpointed `-wal` sidecar (the store never closed cleanly since that write) is invisible to this read-only reader, which reads only the main file's own last-checkpointed content — reproduced directly: a second row written by a still-open writer connection did not appear through a concurrent immutable read, while the first (already-checkpointed) row did. This can only ever make a dry run *under*-report cache hits, never over-report — never a correctness risk for the required guarantee — and never affects the ordinary case this task's own tests verify: two separate CLI invocations, each of which closes its own store connection cleanly (`runCli`'s existing `finally`).
    - **Domain** (`src/domain/estimate.ts`): `estimateDryRun` gained one optional, trailing parameter, `cacheHitTestCaseIds?: ReadonlySet<TestCaseId>` — plain data, no port, no I/O, keeping the function pure. A test case whose id is in the set is excluded from `initialCalls`/`followUpCalls`/`estimatedInputTokens`/`estimatedFollowUpInputTokens`/`estimatedUsd` and counted in a new `cacheHits` field instead; `evaluable`/`evidenceBytes`/`requestBytes`/`rubricBytesPerRequest`/`bundlesOverCeiling` stay scoped to every evaluable test case regardless (not named in this task's own "initialCalls, estimated tokens, and estimated cost" list, and they describe the discovered work itself, not what would be billed). `cacheHits` is present (even as `0`, "consulted, found nothing") only when a set was supplied at all — omitted entirely when `undefined` ("not consulted"), which is also exactly how the math reduces to today's behavior with no separate code branch: an absent set makes every evaluable item fall into the billable path, identically to before this task.
    - **Application** (`src/application/audit.ts`): new `computeDryRunCacheHits(files, sourceTextByPath, cacheKeyPort, lookup)` reuses the existing private `collectEvaluableItems` (the same "what counts as evaluable" `runEvaluation` already uses) and, per evaluable item, calls the caller-supplied `AuditCacheKeyPort.computeKey` then `lookup` — the identical two calls a real dispatch makes before evaluating. Reusing the exact functions with the exact inputs, rather than independently re-deriving an equivalent key, is what guarantees a dry run's billable count agrees with a subsequent real `--evaluate` run by construction, not by careful duplication that could drift. New `RunAuditOptions.retainSourceText?: boolean` (default `false`) makes `runAudit` build (or reuse the already-built) per-file `sourceTextByPath` map and expose it on `AuditResult.sourceTextByPath` — an ordinary audit, and even `--evaluate` without this option, sees no new field, verified by a dedicated test that first tripped over a mutation (below).
    - **Adapter** (`src/adapters/sqlite-audit-store.ts`): new `openSqliteAuditStoreForLookup(options)` returns `AuditStoreReadOnlyLookup | undefined` — `stat`s the file first and returns `undefined` immediately if absent, never loading `node:sqlite` at all for that path (no `ExperimentalWarning` possible, structurally, not just by convention). Schema compatibility is decided by one question — "what would a subsequent real `--evaluate` against this exact file do?" — never a separate policy: a version newer than `SCHEMA_VERSION`, or anything `readSchemaVersion` already treats as corrupt/foreign, throws the identical named error (`AuditStoreSchemaVersionError`/`AuditStoreCorruptError`) a writable `--evaluate` open of the same file would also throw, reusing the CLI's existing catch unchanged; a version *older* than `SCHEMA_VERSION` (no `cache_key` column can exist yet, so no hit is possible) degrades to `undefined` instead, because a real `--evaluate` would just migrate it forward and dispatch everything — the same outcome "not consulted" already reports. The live store's own `lookup` and this reader's `lookup` now share one module-level `LOOKUP_CACHED_JUDGMENT_SQL` constant instead of two hand-copied query strings, so they can never silently drift.
    - **CLI** (`src/cli/index.ts`): gated exactly like `--evaluate`'s own store construction (never for the `dependencies.audit` test seam, and only for `--dry-run`) — opens the read-only lookup before the pipeline runs, passes `retainSourceText: true` to `runAudit` only when a store was actually found (so the common cold path pays no extra memory), computes the hit set after the pipeline runs, and closes the handle in the same `finally` that already closes the writable `--evaluate` store. `dryRunJsonLine`/`dryRunTextReport` splice in `cacheHits`/a "Cache hits" line, conditionally, immediately after `initialCalls` — every other line's bytes are untouched.
    - **The "byte-for-byte identical to today's output" vs. "saying plainly no cache was consulted" tension, surfaced rather than silently resolved.** The task text states both, and read literally they conflict for the cold case (no store exists yet): an always-present "no cache was consulted" line would change today's bytes. Raised with the advisor before implementing; resolved as: keep `--dry-run` and `--dry-run --json`'s bytes genuinely identical to before this task in the cold case (verified — the existing exact-string golden `--dry-run --json` test, and the existing `.toContain()`-based text-report test, both still pass unchanged; a new test additionally asserts `'cacheHits' in parsed` is `false` for a cold run), and document the cold behavior in `--help` and this README instead of in the runtime report. **This is a judgment call, not a settled one** — reported to the orchestrator as a decision gap rather than assumed correct.
    - **Mutation testing** (break, confirm RED, restore) covered: the cache-hit-vs-billable predicate in `estimateDryRun`; the conditional presence of `cacheHits` in `estimateDryRun`'s return (caught by the existing golden `.toEqual()` fixture test too); `followUpCalls`'s billable-only scoping; `openSqliteAuditStoreForLookup`'s stat-first missing-file gate (confirmed it structurally prevents any `node:sqlite` load — proved directly by forcing the load anyway with the gate still returning `undefined` for a missing file, and separately by removing the gate itself and observing a raw native-open failure); the `version < SCHEMA_VERSION` degrade-to-`undefined` branch; `computeDryRunCacheHits`'s hit-vs-miss check; its missing-source-text skip guard; `dryRunJsonLine`'s `cacheHits` placement (moved to the end of the object literal, confirmed the CLI-level warm-dry-run test's key-order assertion turns RED). **Two gaps this task's own verification pass caught in itself, not by an outside reviewer, both corrected before this report**:
      1. The first version of the "omits `sourceTextByPath` when `retainSourceText` is not set" test could not actually discriminate a real defect — in the one scenario it exercised (a plain offline audit, no evaluation port), the internal map is `undefined` regardless of the exposure gate, so mutating the exposure gate to "always expose" still passed. Caught by running that exact mutation and observing it stay GREEN; fixed by adding a second test using a real `--evaluate` port (where the map genuinely IS built, non-empty, for the port's own internal cache-key lookups) — that mutation now turns the strengthened test RED.
      2. A cold-dry-run test originally asserted "never emits the sqlite `ExperimentalWarning`" via a `process.emitWarning` spy installed inside the test body. Proved vacuous by forcing `openSqliteAuditStoreForLookup` to call `loadSqliteModule()` unconditionally (even for a missing file) and observing the spy assertion stay GREEN regardless — because this test file's own top-level `import { DatabaseSync } from 'node:sqlite'` (needed by an unrelated schema-version test) already loads the module, and Node dedupes that exact warning once per process, before any test body's spy is ever installed; `withSqliteExperimentalWarningSuppressed` also swaps `process.emitWarning` out during its own import, so the spy would not even be watching the right function. Removed the spy assertion rather than leave a test that cannot go red; the real guarantee rests on the stat-first gate being structurally load-bearing (mutation-tested per above) plus P5-1's existing fresh-child-process proof of the suppression mechanism itself (`test/bin-smoke.test.ts`), not on an in-process observation that this exact codebase's own test-file layout makes impossible to obtain honestly.
      Named here directly because this phase has repeatedly warned that a fixture which cannot discriminate a real defect is worse than no test.
    - **Proving the billable count matches real dispatches** (the acceptance criterion the task calls out as mattering most): `test/cli.test.ts`'s "THE central acceptance test" runs a real `--evaluate` over one file (2 test cases, real store, real `AuditCacheKeyPort`), adds a second, never-evaluated file to the same root, runs a real `--dry-run --json` and reads its `initialCalls`, then runs a second real `--evaluate` over the same root and store and counts which test names its evaluation port was actually called with. The dispatched-name list (`['multiplies']`, the one new test case) is asserted directly, and `dryRunParsed.initialCalls` is asserted equal to `realDispatches.length` — a count of real calls, never two numbers the estimator derived from itself. A companion test hashes the store file (SHA-256) and checks for `-wal`/`-shm` sidecars before and after the warm dry run, confirming byte-for-byte identity and no new sidecar file.
    - **Fixture discipline** (this phase's own carried-forward warning, applied directly): every new fixture gives its evaluable test cases distinct, non-symmetric values — `estimateDryRun`'s own test picks 3 evaluable / 1 skipped / 1 cache hit / 2 billable (all four counts distinct) with three different bundle content lengths, and deliberately hits the *middle* test case, not the first or last; the CLI-level tests use 3 evaluable / 2 hits / 1 billable, added via a second file written to the fixture root after the warm-up run so the file-level cache key (which covers the whole file's normalized source, not just the changed test) stays valid for the already-cached file.
    - Honestly reported, not silently worked around: this task's self-verification and mutation testing were run by the same worker who implemented it — no independent orchestrator spot check has been performed on this work as of this report, consistent with how prior tasks in this phase flagged the same gap for their own first pass. The schema-version discrimination behavior above (older-than-current degrades silently, newer-or-corrupt surfaces a visible error) is a reasoned interpretation of "what would `--evaluate` do next," not something the task text specifies explicitly — flagged as a second decision gap alongside the byte-identity one above. The known `immutable=1`/uncheckpointed-WAL limitation (a dry run run immediately after an abnormally terminated `--evaluate`, without a clean reopen in between, can under-report hits) is disclosed above and in `docs/technical-design.md`, not fixed — fixing it would mean either dropping the no-sidecar-creation guarantee or adding WAL-recovery logic outside this task's scope.
    - **Both decision gaps immediately above resolved (orchestrator decision, 2026-09-20; implemented, not yet committed)**: disclose explicitly rather than stay silent. `DryRunEstimate` gained `cacheConsulted: boolean` (always present) and `cacheNotConsultedReason?: 'no-store' | 'schema-outdated'`; `openSqliteAuditStoreForLookup` now returns a discriminated union instead of `| undefined`, naming the same reason at its own source. The schema-version discrimination itself (silent degrade vs. named error) is confirmed correct and unchanged. Every existing numeric field/value stays byte-identical; the one deliberate change is the golden `--dry-run --json` fixture gaining `cacheConsulted:false`. Full design, evidence, and mutation testing recorded under "Open questions carried forward" below (now resolved, not left open) and in `docs/technical-design.md`.

## Progress

- Current task: **P5-5 — done; phase complete**, pending orchestrator review and commit.
- Completed tasks: **P5-1**, **P5-2**, **P5-3**, **P5-4**, **P5-5**.
- Running authored count: **~8,095** (1,827 after P5-1 + ~1,301 for P5-2 + ~1,677 for P5-3 + ~1,553 for P5-4 + ~686 for the rootDir-identity defect fix + ~1,051 for P5-5), well over the 3,000-line phase forecast — expected, per this document's own note that Phases 3 and 4 overran similarly.
- Slice ledger:
  - `feat/phase-5-sqlite-store`: `8330fe7` + `5934ab7` — store port, `node:sqlite` adapter with versioned transactional migrations, per-item terminal-state persistence, and lazy CLI wiring.
  - `feat/phase-5-cache-key`: `86d1416` — content-addressed cache key (`src/adapters/cache-key.ts`), schema version 2 (`work_items.cache_key`), `AuditStorePort.lookup`, cache-aware `runEvaluation`, and `--fresh`.
  - `feat/phase-5-scheduler`: `aec02f8` — adaptive concurrency controller (`src/domain/scheduler.ts`), the dynamic dispatcher and request/token budget gate (`src/application/scheduler.ts`, replacing `runBoundedPool`), `ResolvedConfiguration.schedule`, and durable `pending`/`running` checkpoints (`AuditStoreWorkItemOutcome`).
  - `feat/phase-5-resume`: `2ae8caf` + `7b80a8e` — `AuditStorePort.loadRunState`, `--resume <runId>` preflight and completion in `runAudit`/`runEvaluation`, three new named resume error types, and CLI wiring (`--resume` parsing, error handling, `Nothing to resume` reporting, `resume` field in `--evaluate`/`--evaluate --json`); plus, found and fixed in the same slice before commit, the rootDir-identity defect fix — see "Open questions carried forward" below for the full design, symlink, and backward-compatibility decisions.
  - `feat/phase-5-cache-aware-estimate`, off `feat/phase-5-resume`: not yet committed — cache-aware `estimateDryRun` (`cacheHitTestCaseIds`/`cacheHits`), `computeDryRunCacheHits`/`RunAuditOptions.retainSourceText`/`AuditResult.sourceTextByPath` (`src/application/audit.ts`), the read-only `openSqliteAuditStoreForLookup` adapter (`src/adapters/sqlite-audit-store.ts`), and CLI wiring/reporting (`src/cli/index.ts`) — see P5-5's own evidence bullet above for the full design, including the `immutable=1` empirical investigation.

## Open questions carried forward

- The store now persists error messages to disk, where Phase 4 kept them as in-memory diagnostics. That raises the stakes on the gateway's existing `redact()` and deserves a deliberate look — still open; P5-2 changed nothing about it.
- `store.databasePath` is documented as a configuration key, but nothing in the CLI argument parser or a configuration-file loader reaches it: like the pre-existing `evidence` keys, it is only settable by a library consumer calling `resolveConfiguration()`. The gap predates this phase and was not introduced by P5-1 or P5-2; still open.
- Resolved by P5-5, not left open: the P5-2 note immediately above this line — `estimateDryRun` needing `AuditCacheKeyPort`/`store.lookup` and `sourceTextByPath` exposure — is addressed (`computeDryRunCacheHits`, `RunAuditOptions.retainSourceText`, `AuditResult.sourceTextByPath`; see P5-5's own evidence bullet above for the full design).
- Resolved by the orchestrator, not left open: `AuditEvaluationTotals.cached` and excluding a cache hit's usage from run totals are correct. A hit spends nothing on that run, so run usage must report actual spend; the hit's original cost is already recorded on the run that paid it. Likewise `AuditStoreWorkItemOutcome.cacheKey` staying optional on `completed` is acceptable, because production always supplies it — the store is built only under `--evaluate` and the cache-key port is constructed together with it, so only test fakes ever omit it, and a keyless row can never be returned by `lookup`.
- New from P5-3: `ResolvedConfiguration.schedule` (`requestsPerMinute`/`tokensPerSecond`) has the same gap as `store.databasePath` — nothing in the CLI argument parser reaches it; only a library consumer calling `resolveConfiguration()` directly can override it. Consistent with existing precedent, not a regression, but now two configuration sections share this gap instead of one.
- New from P5-3: the `pending` checkpoint loop writes one sequential `recordWorkItem` transaction per evaluable item before the scheduler dispatches anything at all. Fine at the scale this project audits at today; a candidate for batching into fewer transactions if a very large test suite makes that loop's own latency noticeable before evaluation even starts.
- New from P5-3, honestly unresolved by this task alone: the two "neutral vs. clean" distinctions (a non-throttle failure, and a cache hit) initially shipped with tests that could not actually fail on a wrong implementation — caught only by re-running them against a deliberately broken implementation during this task's own mutation-testing pass, not by an independent reviewer. Both are now genuinely discriminating (see P5-3's evidence bullet), but this task's own report is the only verification on record; an independent spot check has not yet re-run these specific mutations.
- Resolved by P5-4, not left open: the two P5-3 open questions immediately above this line — "no `AuditStorePort` method exists yet to read a run's rows back" and "`runAudit` always calls `beginRun`, minting a fresh UUID" — are both addressed (`loadRunState`, and continuing a run by simply skipping `beginRun`; see P5-4's own evidence bullet above for the full design).
- **Closed by the defect fix of 2026-09-20** (found during orchestrator review of P5-4, before commit): `rootDir` was compared as a raw, unnormalized string for the resume root-mismatch check (`state.rootDir !== request.rootDir`) — neither `getResolvedConfiguration` nor `AuditStorePort.beginRun` ever resolved or canonicalized it. The false-reject named here was real, but independent verification found the same root cause also produced a more serious **false accept**: two DIFFERENT repositories, each audited with no `--rootDir` (both defaulting to the literal `"."`), compared EQUAL — a `--resume` against repository B, using a run id minted for repository A, was silently accepted, with B's work items written into A's run and A's cached judgments consulted for tests that do not exist in B.
  - **Fix, narrowest correct layer:** `AuditStorePort` gained `canonicalizeRootDir(rootDir): Promise<string>` (sqlite adapter: `resolve()` then `realpath()`, following symlinks; never throws, falling back to the resolved-but-unreal form when the path cannot be realpath'd). `runAudit` canonicalizes BEFORE calling `beginRun` for a fresh run (so what is PERSISTED is already canonical, never re-derived later), and `preflightResume` canonicalizes the resume request's `rootDir` the same way before comparing. Canonicalizing only at compare time was rejected outright: a stored `"."` would resolve against the resume-time working directory — exactly the false-accept scenario, since `"."` would then resolve to repository B and match. Canonicalizing inside `getResolvedConfiguration` was also rejected: `rootDir` flows into discovery, the source reader, and evidence provenance, none of which this defect touches, so that would have been a wider change than the fix required.
  - **Symlink decision:** real-path resolution (not `resolve()` alone) is necessary, verified empirically — this project's own dev machine confirmed macOS routes its temp directory through a symlinked ancestor (`/var` -> `/private/var`), and a dedicated test (a repository reached through a symlinked ancestor directory, built explicitly rather than relying on that OS-specific layout) fails under `resolve()`-only and passes with `realpath()`.
  - **Backward compatibility, decided and tested:** a run recorded before this fix has a non-canonical `root_dir` (most commonly `"."`) that can never be safely re-resolved (the same working-directory mistake as above). `AuditStoreRunState` gained `rootDirCanonical: boolean` (`isAbsolute(root_dir)` — necessary, not sufficient: a pre-fix run whose `--rootDir` happened to already be absolute reads as canonical, falling through to an ordinary, still-visible mismatch error rather than the legacy one if it no longer matches). `preflightResume` checks this before the mismatch comparison and throws the new, named `AuditResumeLegacyRootDirError` (usage error, exit 1) rather than guessing — never a silent fresh start, never confused with a genuine mismatch. Covered end-to-end against a real `node:sqlite` database seeded with a raw pre-fix row.
  - Evidence: implemented on `feat/phase-5-resume` alongside P5-4 itself, not yet committed — left in the working tree per this task's own instructions. Every required scenario (false accept, false reject across relative/absolute/trailing-slash/symlinked-ancestor spellings, the legacy run, and the pre-existing genuine-mismatch case) was written first and observed RED against the pre-fix code, then made to pass; the persist-at-write-time-not-compare-time principle was itself proven with a dedicated mutation (reverting `beginRun`'s call site alone still leaves the system safe via the independent legacy check, so both halves of the fix were mutation-tested separately, not just together).
- New from P5-4, honestly unresolved by this task alone: this task's self-verification and mutation testing (see its own evidence bullet above) were run by the same worker who implemented it — no independent orchestrator spot check has been performed on this work as of this report.
- **Resolved (orchestrator decision, 2026-09-20; implemented, not yet committed — see below), not left open**: the byte-identity-vs-disclosure conflict P5-5 itself flagged immediately above resolves as **disclose, not stay silent**. The tension was real (the task text asked for both an always-unchanged cold `--dry-run` and a plain "not consulted" disclosure), and it resolves toward the phase's own ethos — "reporting honestly, including reporting what was *not* done and why, is this project's whole ethos" — over byte-identity for its own sake. `DryRunEstimate` gained `cacheConsulted: boolean` (always present) and `cacheNotConsultedReason?: 'no-store' | 'schema-outdated'` (present only when `cacheConsulted` is `false` and a reason is known); `estimateDryRun` gained a matching fifth, optional parameter. The existing exact-match golden `--dry-run --json` test (`test/cli.test.ts`) was updated deliberately to the new expected bytes, not loosened to a substring match, per this follow-up's own instruction. Every existing numeric field and value stays byte-identical; only the disclosure was added. See `docs/technical-design.md`'s "Delivered (orchestrator decision, 2026-09-20): explicit cache-consultation disclosure" for the full design.
- **Resolved (orchestrator decision, 2026-09-20), not left open**: the schema-version discrimination `openSqliteAuditStoreForLookup` applies (older-than-current schema silently degrades to "not consulted"; newer-than-current or corrupt surfaces the same named, visible error a subsequent `--evaluate` would also throw) is confirmed correct and unchanged — only the "not consulted" side gained an explicit reason (`'schema-outdated'`) rather than being reinterpreted. `openSqliteAuditStoreForLookup` itself now returns a discriminated union (`{ available: true; lookup } | { available: false; reason }`) instead of `| undefined`, so the one function that knows why a lookup is unavailable is also the one function that reports it — the CLI passes `reason` straight through to `estimateDryRun` with no re-derivation. A store newer than this build, or corrupt/foreign, still throws the identical named, visible error (`AuditStoreSchemaVersionError`/`AuditStoreCorruptError`) a real `--evaluate` against the same file would also raise — unchanged, per this follow-up's own explicit instruction not to touch that discrimination.
  - Refactoring that return shape from a loose `| undefined` to a checked discriminated union exposed a pre-existing self-deceiving test in `test/sqlite-audit-store.test.ts` (`openSqliteAuditStoreForLookup`'s "uncheckpointed WAL sidecar" test): it used `lookup?.lookup(...)`, so an unavailable lookup (the schema itself, also only in the WAL, was invisible too) and a genuine miss both produced `hit === undefined` — the assertion could not discriminate the two, the same "vacuous by construction" pattern this phase has repeatedly warned about (P5-1's symmetric fixtures, P5-2's under-varying tests, P5-3's `neutral`-vs-`clean` tests, P5-4's single-`rootDir` tests, P5-5's own vacuous warning-spy test). Fixed by checkpointing the schema in a separate bootstrap-and-close step before the real, never-closed write, isolating the row-visibility question the test actually claims to prove; verified by reverting the fix and observing the test fail with `TypeError`/`available: false` where it previously passed vacuously.
- Honestly reported, not silently worked around: this follow-up's own self-verification and mutation testing (break, confirm RED, restore, for the domain field-inclusion guards, the adapter's `no-store`/`schema-outdated` discrimination, and the CLI's reason-passthrough and `available` branch) were run by the same worker who implemented it — no independent orchestrator spot check has been performed on this work as of this report, consistent with the same gap every other task in this phase has flagged for its own first pass. Full suite 850/850 (841 baseline + 9 new tests), typecheck, lint, build, and `git diff --check` all passed; hand-verified against the built CLI for a cold dry run (no store), a warm dry run (seeded store, byte-identical hash/size, no sidecar), a schema-outdated store (byte-identical, `initialCalls` back to fully billable, exit 0), and confirmed the newer-schema/corrupt-store error path is unchanged (exit 1, named error).
- New from P5-5, a known and disclosed (not hidden) limitation: `immutable=1` reads only the main database file's last-checkpointed content, so a row sitting only in an uncheckpointed `-wal` sidecar (the store did not close cleanly since that write) is invisible to a dry run consulting it — this can only ever *under*-report cache hits, never over-report, and never affects the ordinary case of two separate, cleanly-closed CLI invocations that this task's own tests verify. Not fixed, and not in scope to fix: doing so would mean either dropping the no-sidecar-creation guarantee or adding WAL-recovery logic.
- New from P5-5, honestly unresolved by this task alone: this task's self-verification and mutation testing (see its own evidence bullet above) were run by the same worker who implemented it — no independent orchestrator spot check has been performed on this work as of this report, consistent with the same gap every other task in this phase has flagged for its own first pass.

## Next step

Orchestrator reviews P5-5 (cache-aware `--dry-run`) on `feat/phase-5-cache-aware-estimate`, off `feat/phase-5-resume` — in particular the two decision gaps flagged immediately above (the byte-identity-vs-disclosure reading, and the schema-version discrimination behavior) — and commits it. This is the phase's last planned task: once P5-5 is reviewed and committed, Phase 5 ("Persistence, caching, and resilience") is complete, subject to the still-open items listed above (none of which block this task's own acceptance criteria).
